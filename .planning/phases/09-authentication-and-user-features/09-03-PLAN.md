---
phase: 09-authentication-and-user-features
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/components/layout/HeaderBar.tsx
  - src/components/layout/HeaderBar.module.css
  - src/components/auth/AuthButton.tsx
  - src/app/layout.tsx
autonomous: true
requirements: [AUTH-02, AUTH-03]

must_haves:
  truths:
    - "Logged-out users see a Login link in the header"
    - "Logged-in users see their username and a Logout button in the header"
    - "Clicking Logout signs the user out and returns to non-authenticated state"
    - "Session persists across browser refreshes (cookie-based)"
  artifacts:
    - path: "src/components/auth/AuthButton.tsx"
      provides: "Auth-aware header button showing login link or username + logout"
      min_lines: 25
    - path: "src/components/layout/HeaderBar.tsx"
      provides: "Updated HeaderBar with AuthButton"
      contains: "AuthButton"
  key_links:
    - from: "src/components/auth/AuthButton.tsx"
      to: "src/lib/auth-client.ts"
      via: "useSession hook for auth state, signOut for logout"
      pattern: "useSession|signOut"
    - from: "src/components/layout/HeaderBar.tsx"
      to: "src/components/auth/AuthButton.tsx"
      via: "Renders AuthButton in header row"
      pattern: "AuthButton"
---

<objective>
Add auth-aware login/logout button to the header bar visible on every page.

Purpose: Users need to see their auth state and be able to log out from any page (AUTH-03). Session persistence across refreshes (AUTH-02) is verified here.
Output: HeaderBar shows Login link when logged out, username + Logout when logged in.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-authentication-and-user-features/09-01-SUMMARY.md
@src/components/layout/HeaderBar.tsx
@src/components/layout/HeaderBar.module.css
@src/app/layout.tsx
@src/lib/auth-client.ts

<interfaces>
From src/lib/auth-client.ts (created in Plan 01):
```typescript
export const { useSession, signIn, signUp, signOut } = authClient;
// useSession() returns { data: { user: { id, name, email, ... } } | null, isPending }
```

From src/components/layout/HeaderBar.tsx (existing):
```typescript
export function HeaderBar() {
  // Currently a server component — renders logo + date in a table
  // No "use client" directive
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AuthButton component</name>
  <files>src/components/auth/AuthButton.tsx</files>
  <action>
1. Create `src/components/auth/AuthButton.tsx` ("use client"):
   - Import `useSession` and `signOut` from "@/lib/auth-client"
   - Import `useRouter` from "next/navigation"
   - Import Link from "next/link"
   - The component calls `useSession()` to get current session state
   - If `isPending`: render nothing or a small loading placeholder (no layout shift)
   - If not authenticated (session is null/undefined):
     - Render: `<Link href="/auth/login">Login</Link>` styled as plain text link
     - Style: color white, font-size var(--rotter-size-sm), font-family var(--rotter-font-primary)
   - If authenticated:
     - Render: "Welcome, {user.name}" text + a "Logout" button
     - On Logout click: `await signOut()` then `router.refresh()` to update server components
     - Style: same white text, Logout as text-styled button (no box, just text with cursor pointer)
   - The component renders inline (no block layout) so it fits inside a table cell
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>
  - AuthButton renders Login link when no session
  - AuthButton renders username + Logout when session exists
  - signOut call clears the session
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate AuthButton into HeaderBar</name>
  <files>src/components/layout/HeaderBar.tsx, src/components/layout/HeaderBar.module.css, src/app/layout.tsx</files>
  <action>
1. Update `src/components/layout/HeaderBar.tsx`:
   - The current HeaderBar is a SERVER component (no "use client"). AuthButton requires "use client" because it uses hooks.
   - Solution: Import AuthButton and render it inside the header table. Since AuthButton is a client component imported into a server component, Next.js handles this correctly (client components can be imported into server components).
   - Add a third `<td>` in the header table row (after date cell) that renders `<AuthButton />`
   - Style the new cell: text-align right, vertical-align middle, padding-right 10px
   - AuthButton renders white text against the gradient header background — ensure contrast

2. Update `src/components/layout/HeaderBar.module.css`:
   - Add `.authCell` class: text-align right, vertical-align middle, white-space nowrap

3. Verify `src/app/layout.tsx` does NOT need changes — HeaderBar is already rendered in page-level server components, not in layout.tsx. The auth session cookie is automatically sent with requests, so no provider wrapper is needed in layout.tsx.
   - HOWEVER: Check if Better Auth requires a session provider in the React tree. If `useSession` requires a `<SessionProvider>` or `<AuthProvider>` wrapper, add it to `src/app/layout.tsx` body. The provider would be a separate client component wrapping `{children}`.
   - If a provider IS needed: Create `src/components/auth/AuthProvider.tsx` ("use client") that wraps children with Better Auth's provider, then import it in layout.tsx.
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>
  - HeaderBar displays AuthButton in the right side of the header
  - AuthButton shows Login link for unauthenticated users
  - AuthButton shows username + Logout for authenticated users
  - No hydration errors in the browser console
  - Build passes
  </done>
</task>

</tasks>

<verification>
- `pnpm build` completes without errors
- `grep "AuthButton" src/components/layout/HeaderBar.tsx` confirms integration
- `grep "useSession\|signOut" src/components/auth/AuthButton.tsx` confirms auth hooks used
- `grep "use client" src/components/auth/AuthButton.tsx` confirms client component
</verification>

<success_criteria>
Header shows auth state on every page. Login link navigates to /auth/login. Logout button clears session. Session persists across page refreshes (cookie-based via Better Auth). Build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/09-authentication-and-user-features/09-03-SUMMARY.md`
</output>
