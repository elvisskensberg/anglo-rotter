---
phase: 10-polish-and-launch
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/og-image.png
  - public/robots.txt
autonomous: true
requirements:
  - POLISH-01
  - POLISH-04
must_haves:
  truths:
    - "A default OG image exists at /og-image.png with the Rotter blue theme"
    - "robots.txt allows crawling of all public pages and blocks /api/ routes"
  artifacts:
    - path: "public/og-image.png"
      provides: "Default Open Graph image for social sharing"
    - path: "public/robots.txt"
      provides: "Search engine crawling directives"
      contains: "User-agent"
  key_links:
    - from: "src/app/layout.tsx"
      to: "public/og-image.png"
      via: "openGraph.images[0].url = '/og-image.png'"
      pattern: "og-image.png"
---

<objective>
Create the static OG image and robots.txt for SEO completeness.

Purpose: The OG image is referenced by the root layout metadata (Plan 01). robots.txt guides search engine crawlers. These are static assets that can be created independently.
Output: public/og-image.png (1200x630) and public/robots.txt
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate OG image and robots.txt</name>
  <files>public/og-image.png, public/robots.txt</files>
  <read_first>data/design/DESIGN_SPECIFICATION.md (Section 8 Color Scheme)</read_first>
  <action>
**OG Image (public/og-image.png):**
Create a 1200x630 PNG image programmatically using a Node.js script (inline, no new dependency). The image should be:
- Background: solid #71B7E6 (the Rotter.net main header blue)
- White text centered: "MultiRotter" in large bold sans-serif
- Subtitle below: "Breaking News Scoops Forum" in smaller white text

Since we have no canvas library installed, generate the OG image using Next.js built-in `ImageResponse` from `next/og`. Create a temporary script OR use a simpler approach:

Option A (preferred — no dependencies): Create a simple SVG-based PNG. Write a Node script that:
1. Creates an SVG string with the blue background (#71B7E6), white "MultiRotter" text (48px bold), and "Breaking News Scoops Forum" subtitle (24px)
2. Use the `sharp` package if available, OR simply save the SVG as `public/og-image.svg` and reference it in metadata instead

Option B (simplest — just use SVG): Create `public/og-image.svg` instead of PNG:
```svg
<svg xmlns="http://www.w3.org/2000/svg" width="1200" height="630" viewBox="0 0 1200 630">
  <rect width="1200" height="630" fill="#71B7E6"/>
  <text x="600" y="280" text-anchor="middle" font-family="Arial, sans-serif" font-size="72" font-weight="bold" fill="white">MultiRotter</text>
  <text x="600" y="340" text-anchor="middle" font-family="Arial, sans-serif" font-size="32" fill="white">Breaking News Scoops Forum</text>
</svg>
```

Use Option B (SVG). Save as `public/og-image.svg`. Then update the OG image reference to `/og-image.svg` in layout.tsx (coordinate with Plan 01 — if Plan 01 already ran, edit layout.tsx to change `.png` to `.svg`; if not yet, Plan 01 should use `.svg`).

Actually — OG images must be PNG/JPG for most social platforms (Twitter, Facebook reject SVG). So use this approach instead:

Create `public/og-image.png` using Next.js `next/og` ImageResponse in an API route at build time. But that's overly complex for a static image.

**Simplest reliable approach:** Create a minimal HTML file, screenshot it, save as PNG. But we have no browser automation in the plan.

**Final approach — use pure Node.js buffer manipulation to create a valid 1200x630 solid-color PNG:**
Write a Node.js script `scripts/generate-og-image.mjs` that:
1. Generates a minimal valid PNG (solid #71B7E6 background, 1200x630)
2. Uses only built-in Node.js modules (no sharp, no canvas)
3. A solid-color PNG with no text is acceptable — the blue branding color is recognizable
4. Saves to public/og-image.png
5. Run the script: `node scripts/generate-og-image.mjs`

The solid blue rectangle is sufficient for v1 — it matches the Rotter.net header color and provides visual branding in social shares. Text overlay can be added in a future iteration if needed.

**robots.txt (public/robots.txt):**
Create with content:
```
User-agent: *
Allow: /
Disallow: /api/
Disallow: /auth/

Sitemap: https://multirotter.vercel.app/sitemap.xml
```

This allows crawling of all public pages while blocking API endpoints and auth pages.
  </action>
  <verify>
    <automated>cd /c/Code/MultiRotter && test -f public/og-image.png && test -f public/robots.txt && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - test -f public/og-image.png (file exists and is > 0 bytes)
    - grep -q "User-agent" public/robots.txt
    - grep -q "Disallow: /api/" public/robots.txt
  </acceptance_criteria>
  <done>public/og-image.png exists as a 1200x630 solid #71B7E6 PNG. public/robots.txt blocks /api/ and /auth/ paths.</done>
</task>

</tasks>

<verification>
- `ls -la public/og-image.png` shows file exists with reasonable size
- `cat public/robots.txt` shows correct crawling rules
</verification>

<success_criteria>
Static OG image and robots.txt exist in the public directory, ready to be served by Next.js.
</success_criteria>

<output>
After completion, create `.planning/phases/10-polish-and-launch/10-02-SUMMARY.md`
</output>
