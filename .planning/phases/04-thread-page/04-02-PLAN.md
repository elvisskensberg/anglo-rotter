---
phase: 04-thread-page
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/thread/ThreadBreadcrumb.tsx
  - src/components/thread/ThreadBreadcrumb.module.css
  - src/components/thread/OriginalPostBlock.tsx
  - src/components/thread/OriginalPostBlock.module.css
  - src/components/thread/ActionButtons.tsx
  - src/components/thread/ActionButtons.module.css
autonomous: true
requirements: [THRD-01, THRD-02, THRD-03, THRD-08]

must_haves:
  truths:
    - "Breadcrumb bar renders Forums > Section > Thread #N with navigable links on #3293CD background"
    - "Original post block shows author name, star rating SVG, member since, post count, raters/points on #eeeeee"
    - "Post content appears in #FDFDFD area with 16px bold h1 and 70%-width nested table"
    - "Action buttons row renders edit, up, reply, view all, back as text links"
  artifacts:
    - path: "src/components/thread/ThreadBreadcrumb.tsx"
      provides: "THRD-08 breadcrumb navigation"
      exports: ["ThreadBreadcrumb"]
    - path: "src/components/thread/OriginalPostBlock.tsx"
      provides: "THRD-01 author info + THRD-02 post content"
      exports: ["OriginalPostBlock"]
    - path: "src/components/thread/ActionButtons.tsx"
      provides: "THRD-03 action buttons row"
      exports: ["ActionButtons"]
  key_links:
    - from: "src/components/thread/ThreadBreadcrumb.tsx"
      to: "/forum/[forumId]"
      via: "href link to forum listing"
      pattern: "href=.*forum"
    - from: "src/components/thread/OriginalPostBlock.tsx"
      to: "/icons/star-{N}.svg"
      via: "img src interpolation from starRating field"
      pattern: "star-.*svg"
    - from: "src/components/thread/ActionButtons.tsx"
      to: "onReplyClick callback"
      via: "prop passed from ThreadPageClient"
      pattern: "onReplyClick"
---

<objective>
Build the top half of the thread page: breadcrumb bar, original post block (author info + content), and action buttons row.

Purpose: Deliver THRD-01, THRD-02, THRD-03, THRD-08 — the post display and navigation components.
Output: 3 components with CSS modules that pass their respective test stubs from Plan 01.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-thread-page/04-CONTEXT.md
@.planning/phases/04-thread-page/04-RESEARCH.md
@.planning/phases/04-thread-page/04-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 that these components consume -->

From src/data/thread-seed.ts (created in Plan 01):
```typescript
export interface ThreadPost {
  id: number;
  author: string;
  content: string;
  date: string;        // "DD.MM.YY"
  time: string;        // "HH:MM"
  memberSince: string; // "D.M.YY"
  postCount: number;
  starRating: number;  // 1-5
  ratersCount: number;
  points: number;
}

export interface ThreadData {
  id: number;
  forumId: string;     // e.g. "scoops1"
  sectionName: string; // e.g. "Scoops"
  title: string;
  post: ThreadPost;
  replies: ReplyTreeItem[];
}
```

From src/components/ui/Table.tsx:
```typescript
export function Table({ children, className, ...props }: TableProps): JSX.Element;
// MUST wrap all <table> usage. Callers MUST include explicit <tbody>.
```

Available CSS tokens (from globals.css):
- --rotter-subheader-blue: #3293CD (breadcrumb bar)
- --rotter-row-even: #eeeeee (author info row)
- --rotter-row-odd: #FDFDFD (post content area)
- --rotter-text-header: #FFFFFF (breadcrumb text)
- --rotter-text-primary: #000099 (author name, content)
- --rotter-green-rating: #006633 (raters count)
- --rotter-text-time: red (time, points)
- --rotter-size-xs: 9pt, --rotter-size-sm: 12px, --rotter-size-post-title: 16px
- --rotter-font-primary: Arial, Helvetica, sans-serif
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ThreadBreadcrumb component</name>
  <files>src/components/thread/ThreadBreadcrumb.tsx, src/components/thread/ThreadBreadcrumb.module.css</files>
  <read_first>
    - src/__tests__/ThreadBreadcrumb.test.tsx (test expectations to satisfy)
    - .planning/phases/04-thread-page/04-RESEARCH.md (Pattern 5: Breadcrumb Bar, code example)
    - src/components/ui/Table.tsx (Table component signature)
    - data/design/thread_page.html lines 483-500 (original HTML structure)
  </read_first>
  <action>
    Create `src/components/thread/ThreadBreadcrumb.tsx`:

    Props interface: `{ forumId: string; sectionName: string; threadId: number }`

    Structure (per RESEARCH.md Pattern 5, verified from thread_page.html lines 483-500):
    - Outer Table: width="100%", cellSpacing={0}, cellPadding={0}, backgroundColor="#000000" (1px black border)
    - Inner Table: width="100%", cellSpacing={0}, cellPadding={3}
    - Single `<tr>` with backgroundColor "var(--rotter-subheader-blue)"
    - `<th align="right" valign="bottom">` containing:
      - Link "Forums" with href="/" — color: var(--rotter-text-header), fontFamily: var(--rotter-font-primary), fontSize: var(--rotter-size-sm)
      - Space + dir-arrow.svg (8x8 img) + space
      - Link "{sectionName}" with href={`/forum/${forumId}`} — same style
      - Space + dir-arrow.svg (8x8 img) + space
      - Span "Thread #{threadId}" — same style (NOT a link)
    - Both Tables use `<tbody>` explicitly
    - Use `/* eslint-disable-next-line @next/next/no-img-element */` before each `<img>`
    - Import Table from "@/components/ui/Table"
    - Do NOT add 'use client' — this is a presentational component

    CSS Module (`ThreadBreadcrumb.module.css`): minimal, only if needed for .text16b or other class-based styles. Most styling is inline via style prop (matching project pattern from ThreadRow).

    Run tests: `npx jest --testPathPattern="ThreadBreadcrumb" --no-coverage`
  </action>
  <verify>
    <automated>npx jest --testPathPattern="ThreadBreadcrumb" --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - ThreadBreadcrumb renders Forums link (href="/"), section link (href="/forum/{forumId}"), and "Thread #{id}" text
    - Blue background bar uses var(--rotter-subheader-blue)
    - All ThreadBreadcrumb tests pass (GREEN)
  </acceptance_criteria>
  <done>ThreadBreadcrumb.test.tsx passes — THRD-08 complete</done>
</task>

<task type="auto">
  <name>Task 2: Build OriginalPostBlock component</name>
  <files>src/components/thread/OriginalPostBlock.tsx, src/components/thread/OriginalPostBlock.module.css</files>
  <read_first>
    - src/__tests__/OriginalPostBlock.test.tsx (test expectations to satisfy)
    - .planning/phases/04-thread-page/04-RESEARCH.md (Pattern 3: Original Post Block, author info row code example, post content area code example)
    - data/design/thread_page.html lines 521-569 (original HTML structure)
    - src/components/forum/ThreadRow.tsx (inline style pattern reference)
  </read_first>
  <action>
    Create `src/components/thread/OriginalPostBlock.tsx`:

    Props interface: `{ post: ThreadPost; title: string }` — import ThreadPost from "@/data/thread-seed"

    Structure (per RESEARCH.md Pattern 3, verified from thread_page.html lines 521-569):

    **Outer wrapper**: Table with backgroundColor="#000000" (1px border), cellSpacing={0}, cellPadding={0}
    **Inner content table**: cellPadding={3}, cellSpacing={0}

    **Row 1 — Author info** (THRD-01, backgroundColor: var(--rotter-row-even)):
    - td (width="50%", align="right", valign="top", rowSpan={2}):
      - Bold author name: fontSize var(--rotter-size-sm), color var(--rotter-text-primary)
      - Space + star rating: `<img src={"/icons/star-" + post.starRating + ".svg"} alt={post.starRating + " stars"} />` with eslint-disable comment
      - `<br />` + "Member since {post.memberSince}" in fontSize var(--rotter-size-xs)
      - `<br />` + "{post.postCount} posts" in fontSize var(--rotter-size-xs)
      - ", " + "{post.ratersCount} raters" in bold green (var(--rotter-green-rating))
      - ", " + "{post.points} points" in bold red (var(--rotter-text-time))
    - td (width="50%", align="left", valign="top"):
      - Date in var(--rotter-size-xs) + space + time in var(--rotter-text-time)

    **Row 2 — Action icons placeholder** (backgroundColor: var(--rotter-row-even)):
    - Single td with placeholder text or empty (these are send message / profile / buddy icons — not needed for Phase 4)

    **Row 3 — Post content** (THRD-02, backgroundColor: var(--rotter-row-odd)):
    - td with colSpan={2}
    - `<h1>` with className styles.text16b, style margin "0px", paddingRight "10px" — renders title text
    - `<br />`
    - Nested Table: width="70%", display "inline-table", cellPadding={5}, cellSpacing={0}
      - tbody > tr > td (valign="top", width={50}) with "&nbsp;&nbsp;" + td (valign="top", width="100%") with content span
      - Content span: fontSize var(--rotter-size-sm), color var(--rotter-text-primary), fontFamily var(--rotter-font-primary)

    CSS Module (`OriginalPostBlock.module.css`):
    - `.text16b` class: font-size 16px; font-weight bold; font-family var(--rotter-font-primary); color var(--rotter-text-primary)

    All Tables use explicit `<tbody>`. Import Table from "@/components/ui/Table".
    Do NOT add 'use client' — presentational component.

    Run tests: `npx jest --testPathPattern="OriginalPostBlock" --no-coverage`
  </action>
  <verify>
    <automated>npx jest --testPathPattern="OriginalPostBlock" --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - Author info row shows username, star SVG, memberSince, postCount, raters, points with correct colors
    - Post content area has h1 title and 70%-width nested table
    - All OriginalPostBlock tests pass (GREEN)
  </acceptance_criteria>
  <done>OriginalPostBlock.test.tsx passes — THRD-01 and THRD-02 complete</done>
</task>

<task type="auto">
  <name>Task 3: Build ActionButtons component</name>
  <files>src/components/thread/ActionButtons.tsx, src/components/thread/ActionButtons.module.css</files>
  <read_first>
    - src/__tests__/ActionButtons.test.tsx (test expectations to satisfy)
    - .planning/phases/04-thread-page/04-RESEARCH.md (Open Question 2: action button approach — use text links per CONTEXT.md)
    - data/design/thread_page.html lines 558-569 (original action bar HTML)
  </read_first>
  <action>
    Create `src/components/thread/ActionButtons.tsx`:

    Props interface: `{ forumId: string; onReplyClick: () => void }`

    Structure (per CONTEXT.md locked decision — "styled as text links matching Rotter's button bar"):
    - Single `<tr>` with backgroundColor: var(--rotter-row-even)
    - `<td colSpan={2} align="center">` containing 5 text links separated by " | ":
      1. "Edit" — `<a href="#" style={{ fontSize: "var(--rotter-size-xs)", color: "var(--rotter-text-primary)" }}>Edit</a>`
      2. "Up" — `<a href="#top" ...>Up</a>`
      3. "Reply" — `<button onClick={onReplyClick} style={{ background: "none", border: "none", cursor: "pointer", fontSize: "var(--rotter-size-xs)", color: "var(--rotter-text-primary)", fontFamily: "var(--rotter-font-primary)", textDecoration: "none", padding: 0 }}>Reply</button>` (button for click handler, styled as text link)
      4. "View All" — `<a href="#" ...>View All</a>`
      5. "Back" — `<a href={"/forum/" + forumId} ...>Back</a>`

    NOTE: "Reply" is a `<button>` (not `<a>`) because it triggers onReplyClick callback. Style it identically to the link text.
    Do NOT add 'use client' — the click handler is passed as a prop from the client boundary (ThreadPageClient).

    CSS Module: minimal or empty (inline styles match project pattern).

    Run tests: `npx jest --testPathPattern="ActionButtons" --no-coverage`
  </action>
  <verify>
    <automated>npx jest --testPathPattern="ActionButtons" --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - 5 action items rendered: Edit, Up, Reply, View All, Back
    - Reply click calls onReplyClick
    - Back link href points to /forum/{forumId}
    - All ActionButtons tests pass (GREEN)
  </acceptance_criteria>
  <done>ActionButtons.test.tsx passes — THRD-03 complete</done>
</task>

</tasks>

<verification>
- `npx jest --testPathPattern="ThreadBreadcrumb|OriginalPostBlock|ActionButtons" --no-coverage` — all 3 test files pass
- No TypeScript errors: `npx tsc --noEmit --project tsconfig.json`
</verification>

<success_criteria>
- ThreadBreadcrumb renders navigable breadcrumb with Forums > Section > Thread #N (THRD-08)
- OriginalPostBlock renders author info with stars and post content in 70%-width table (THRD-01, THRD-02)
- ActionButtons renders 5 text links with Reply triggering callback (THRD-03)
- All corresponding test files pass GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/04-thread-page/04-02-SUMMARY.md`
</output>
