---
phase: 04-thread-page
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/thread/ReplyTree.tsx
  - src/components/thread/ReplyTree.module.css
  - src/components/thread/ReplyRow.tsx
  - src/components/thread/QuickReplyForm.tsx
  - src/components/thread/QuickReplyForm.module.css
autonomous: true
requirements: [THRD-04, THRD-05, THRD-06, THRD-07]

must_haves:
  truths:
    - "Reply tree table renders 4 column headers: Thread, Author, Date, #"
    - "Top-level replies (depth=1) show message.svg; nested replies (depth>=2) show reply-message.svg"
    - "Indentation uses nbsp characters: depth 1 = 2 nbsp, depth 2 = 4 nbsp, depth 3 = 8 nbsp (formula: depth===1 ? 2 : (depth-1)*4)"
    - "Reply rows alternate between --rotter-row-even and --rotter-row-odd"
    - "Quick reply form is hidden when visible=false and shows textarea+submit when visible=true"
  artifacts:
    - path: "src/components/thread/ReplyTree.tsx"
      provides: "THRD-04, THRD-05, THRD-06 reply tree table"
      exports: ["ReplyTree"]
    - path: "src/components/thread/ReplyRow.tsx"
      provides: "Individual reply row with indentation"
      exports: ["ReplyRow"]
    - path: "src/components/thread/QuickReplyForm.tsx"
      provides: "THRD-07 toggle-visible quick reply form"
      exports: ["QuickReplyForm"]
  key_links:
    - from: "src/components/thread/ReplyRow.tsx"
      to: "/icons/message.svg and /icons/reply-message.svg"
      via: "img src conditional on depth"
      pattern: "depth.*message"
    - from: "src/components/thread/ReplyTree.tsx"
      to: "src/components/thread/ReplyRow.tsx"
      via: "direct import, maps replies array to ReplyRow"
      pattern: "import.*ReplyRow"
---

<objective>
Build the reply tree table and quick reply form — the bottom half of the thread page.

Purpose: Deliver THRD-04, THRD-05, THRD-06, THRD-07 — the reply display and quick reply interaction.
Output: ReplyTree (with ReplyRow), QuickReplyForm components with CSS modules that pass their test stubs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-thread-page/04-CONTEXT.md
@.planning/phases/04-thread-page/04-RESEARCH.md
@.planning/phases/04-thread-page/04-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 -->

From src/data/thread-seed.ts (created in Plan 01):
```typescript
export interface ReplyTreeItem {
  id: number;
  replyNumber: number;
  title: string;
  author: string;
  date: string;        // "DD.MM.YY"
  time: string;        // "HH:MM"
  depth: number;       // 1 = top-level, 2+ = nested
}
```

From src/components/ui/Table.tsx:
```typescript
export function Table({ children, className, ...props }: TableProps): JSX.Element;
```

Available CSS tokens:
- --rotter-row-even: #eeeeee
- --rotter-row-odd: #FDFDFD
- --rotter-text-primary: #000099
- --rotter-text-time: red
- --rotter-size-sm: 12px
- --rotter-size-xs: 9pt
- --rotter-size-thread-title: 15px
- --rotter-font-primary: Arial, Helvetica, sans-serif
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ReplyRow and ReplyTree components</name>
  <files>src/components/thread/ReplyRow.tsx, src/components/thread/ReplyTree.tsx, src/components/thread/ReplyTree.module.css</files>
  <read_first>
    - src/__tests__/ReplyTree.test.tsx (test expectations to satisfy)
    - .planning/phases/04-thread-page/04-RESEARCH.md (Pattern 2: Reply Tree Indentation — verified nbsp formula and code example)
    - data/design/DESIGN_SPECIFICATION.md section 4 (Reply Tree structure)
    - src/components/forum/ThreadRow.tsx (inline style pattern reference)
  </read_first>
  <action>
    **Create `src/components/thread/ReplyRow.tsx`:**

    Props: `{ item: ReplyTreeItem; isEven: boolean }`

    Renders a single `<tr>` with 4 `<td>` cells:

    1. **Thread title cell** (align="right", width="100%", nowrap):
       - Nbsp indentation: `"\u00a0".repeat(item.depth === 1 ? 2 : (item.depth - 1) * 4)`
       - Icon: `<img src={item.depth === 1 ? "/icons/message.svg" : "/icons/reply-message.svg"} alt="" width={16} height={16} />` with eslint-disable comment
       - Space + link: `<a href={"#" + item.replyNumber}` with fontSize var(--rotter-size-thread-title), color var(--rotter-text-primary)> {item.title} </a>

    2. **Author cell** (align="center", nowrap):
       - Span with fontSize var(--rotter-size-sm), color var(--rotter-text-primary): {item.author}

    3. **Date cell** (align="center", nowrap):
       - Date span: fontSize var(--rotter-size-sm), color var(--rotter-text-primary): {item.date}
       - Space + time span: fontSize var(--rotter-size-sm), color var(--rotter-text-time): {item.time}

    4. **Number cell** (align="center", nowrap):
       - Span with fontSize var(--rotter-size-sm), color var(--rotter-text-primary): {item.replyNumber}

    Row backgroundColor: `isEven ? "var(--rotter-row-even)" : "var(--rotter-row-odd)"`

    Do NOT add 'use client'. Use `/* eslint-disable-next-line @next/next/no-img-element */`.

    **Create `src/components/thread/ReplyTree.tsx`:**

    Props: `{ replies: ReplyTreeItem[] }`

    Structure (per DESIGN_SPECIFICATION.md section 4 and RESEARCH.md):
    - Table: width="100%", cellSpacing={1}, cellPadding={1}
    - Header row: backgroundColor var(--rotter-row-even), 4 `<th>` cells:
      - "Thread" (align="right")
      - "Author" (align="center")
      - "Date" (align="center")
      - "#" (align="center")
    - Map replies to ReplyRow components: `replies.map((item, index) => <ReplyRow key={item.id} item={item} isEven={index % 2 === 0} />)`
    - Import Table from "@/components/ui/Table"
    - Explicit `<tbody>` wrapping all rows

    Do NOT add 'use client'.

    CSS Module (`ReplyTree.module.css`): minimal, header row font styles if needed.

    Run tests: `npx jest --testPathPattern="ReplyTree" --no-coverage`
  </action>
  <verify>
    <automated>npx jest --testPathPattern="ReplyTree" --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - ReplyTree renders 4 column headers: "Thread", "Author", "Date", "#"
    - Depth 1 replies use message.svg, depth 2+ use reply-message.svg
    - Depth 3 reply has 8 nbsp chars before icon
    - Alternating row colors applied correctly
    - All ReplyTree tests pass (GREEN)
  </acceptance_criteria>
  <done>ReplyTree.test.tsx passes — THRD-04, THRD-05, THRD-06 complete</done>
</task>

<task type="auto">
  <name>Task 2: Build QuickReplyForm component</name>
  <files>src/components/thread/QuickReplyForm.tsx, src/components/thread/QuickReplyForm.module.css</files>
  <read_first>
    - src/__tests__/QuickReplyForm.test.tsx (test expectations to satisfy)
    - .planning/phases/04-thread-page/04-RESEARCH.md (Pattern 4: Quick Reply Toggle)
    - .planning/phases/04-thread-page/04-CONTEXT.md (Quick Reply Form locked decisions)
  </read_first>
  <action>
    Create `src/components/thread/QuickReplyForm.tsx`:

    Props: `{ visible: boolean }`

    Per CONTEXT.md locked decision:
    - Hidden by default (visible=false returns null)
    - When visible=true, renders:
      - Table wrapper (width="100%", cellPadding={5}, cellSpacing={0}) with explicit tbody
      - Single row with backgroundColor var(--rotter-row-odd)
      - td containing:
        - Label "Quick Reply:" in bold, fontSize var(--rotter-size-sm)
        - `<textarea rows={6} cols={60} />` with fontFamily var(--rotter-font-primary)
        - `<br />`
        - Submit button: `<button type="submit">Submit Reply</button>` styled with fontSize var(--rotter-size-sm), backgroundColor var(--rotter-row-even), border "1px solid #999", cursor "pointer", padding "4px 12px"
    - No actual form submission (Phase 9 adds auth + real posting per CONTEXT.md)
    - Do NOT add 'use client' — visibility is controlled via prop from parent client component

    CSS Module (`QuickReplyForm.module.css`):
    - `.textarea`: width 100%, max-width 500px, font-family inherit, font-size var(--rotter-size-sm)

    Run tests: `npx jest --testPathPattern="QuickReplyForm" --no-coverage`
  </action>
  <verify>
    <automated>npx jest --testPathPattern="QuickReplyForm" --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - QuickReplyForm returns null when visible=false
    - When visible=true, renders textarea and "Submit Reply" button
    - All QuickReplyForm tests pass (GREEN)
  </acceptance_criteria>
  <done>QuickReplyForm.test.tsx passes — THRD-07 complete</done>
</task>

</tasks>

<verification>
- `npx jest --testPathPattern="ReplyTree|QuickReplyForm" --no-coverage` — all tests pass
- No TypeScript errors: `npx tsc --noEmit --project tsconfig.json`
</verification>

<success_criteria>
- ReplyTree renders 4-column table with correct indentation and alternating colors (THRD-04, THRD-05, THRD-06)
- QuickReplyForm toggles visibility correctly (THRD-07)
- All corresponding test files pass GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/04-thread-page/04-03-SUMMARY.md`
</output>
