---
phase: 04-thread-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/thread-seed.ts
  - src/__tests__/thread-seed.test.ts
  - src/__tests__/ThreadBreadcrumb.test.tsx
  - src/__tests__/OriginalPostBlock.test.tsx
  - src/__tests__/ActionButtons.test.tsx
  - src/__tests__/ReplyTree.test.tsx
  - src/__tests__/QuickReplyForm.test.tsx
  - src/__tests__/ThreadPageClient.test.tsx
autonomous: true
requirements: [THRD-01, THRD-02, THRD-03, THRD-04, THRD-05, THRD-06, THRD-07, THRD-08]

must_haves:
  truths:
    - "ThreadData, ThreadPost, and ReplyTreeItem interfaces are exported and typed"
    - "THREAD_SEED contains at least 1 thread with 15+ replies at 3+ depth levels"
    - "All 6 component test files exist with failing tests targeting THRD-01 through THRD-08"
  artifacts:
    - path: "src/data/thread-seed.ts"
      provides: "ThreadData, ThreadPost, ReplyTreeItem types + THREAD_SEED array"
      exports: ["ThreadData", "ThreadPost", "ReplyTreeItem", "THREAD_SEED"]
    - path: "src/__tests__/thread-seed.test.ts"
      provides: "Seed data validation tests"
    - path: "src/__tests__/ThreadBreadcrumb.test.tsx"
      provides: "THRD-08 test stub"
    - path: "src/__tests__/OriginalPostBlock.test.tsx"
      provides: "THRD-01, THRD-02 test stubs"
    - path: "src/__tests__/ActionButtons.test.tsx"
      provides: "THRD-03 test stub"
    - path: "src/__tests__/ReplyTree.test.tsx"
      provides: "THRD-04, THRD-05, THRD-06 test stubs"
    - path: "src/__tests__/QuickReplyForm.test.tsx"
      provides: "THRD-07 test stub"
    - path: "src/__tests__/ThreadPageClient.test.tsx"
      provides: "THRD-07 toggle integration test stub"
  key_links:
    - from: "src/data/thread-seed.ts"
      to: "data/threads/threads_with_comments_2026-03-22.json"
      via: "Translated seed data derived from scraped JSON"
      pattern: "THREAD_SEED"
---

<objective>
Create the thread seed data (types + THREAD_SEED constant) and Wave 0 test stubs for all 8 THRD requirements.

Purpose: Establish the data contract and test expectations before any component implementation.
Output: thread-seed.ts with typed interfaces and seed data; 6 failing test files covering THRD-01 through THRD-08.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-thread-page/04-CONTEXT.md
@.planning/phases/04-thread-page/04-RESEARCH.md
@src/data/forum-seed.ts
@data/threads/threads_with_comments_2026-03-22.json

<interfaces>
<!-- Existing types and patterns the executor needs -->

From src/data/forum-seed.ts:
```typescript
export interface ForumThread {
  id: number;
  title: string;
  author: string;
  date: string;        // "DD.MM.YY"
  time: string;        // "HH:MM"
  // ...other fields
  url: string;         // Currently "/forum/scoops1/${id}" — will be updated in Plan 04
}
```

From src/components/ui/Table.tsx:
```typescript
export function Table({ children, className, ...props }: TableProps): JSX.Element;
// All tables MUST use this component with explicit <tbody>
```

Available icons in public/icons/:
- message.svg, reply-message.svg
- star-1.svg through star-5.svg
- dir-arrow.svg
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create thread-seed.ts with types and seed data</name>
  <files>src/data/thread-seed.ts, src/__tests__/thread-seed.test.ts</files>
  <read_first>
    - data/threads/threads_with_comments_2026-03-22.json (scraped reply trees for thread scoops1_940165)
    - src/data/forum-seed.ts (existing seed pattern to follow)
    - .planning/phases/04-thread-page/04-RESEARCH.md (Pattern 6: Seed Data Structure, depth computation helper)
  </read_first>
  <action>
    NOTE: RESEARCH.md Pattern 6 shows a ThreadData snippet that omits the `title` field. That code is illustrative, not authoritative. The interfaces below (from CONTEXT.md decisions) are the source of truth. ThreadData MUST include `title: string`.

    Create `src/data/thread-seed.ts` with:

    1. **Interfaces** (per CONTEXT.md locked decisions):
       - `ThreadPost`: id (number), author (string), content (string), date (string "DD.MM.YY"), time (string "HH:MM"), memberSince (string "D.M.YY"), postCount (number), starRating (number 1-5), ratersCount (number), points (number)
       - `ReplyTreeItem`: id (number), replyNumber (number), title (string), author (string), date (string "DD.MM.YY"), time (string "HH:MM"), depth (number: 1=top-level, 2+=nested)
       - `ThreadData`: id (number), forumId (string e.g. "scoops1"), sectionName (string e.g. "Scoops"), title (string), post (ThreadPost), replies (ReplyTreeItem[])

    2. **Seed data** translated from `data/threads/threads_with_comments_2026-03-22.json` key `scoops1_940165`:
       - Thread 940165: "Supreme Court refuses to broadcast hearing on aid organizations"
       - Original post by "mortzix", memberSince "1.3.26", postCount 6555, starRating 3, ratersCount 45, points 120
       - 35 replies (post_number 1-35), transliterate Hebrew authors to Latin (e.g. "barry white" stays, Hebrew names get transliterated: use simple English equivalents like "TheDreamer" for Hebrew names)
       - Pre-compute depth using BFS from reply_to chains in the JSON:
         - depth 0 = original post (post_number 0, not in replies array)
         - depth 1 = replies to post 0 (reply_to: 0) — use message.svg icon
         - depth 2+ = nested replies (reply_to: non-zero) — use reply-message.svg icon
       - The reply tree from the JSON has posts up to depth 6 (the chain: 5->31->32->33->34->35)
       - Reply titles: use "Re: " + parent title for depth 2+, original thread title for depth 1 (keep short, max 60 chars)
       - Dates: use "22.03.26" for all (same day), fabricate times incrementing from "14:00"

    3. **Add a second seed thread** (thread 940099) with fewer replies (5-8) for simpler testing:
       - "Casualty update from Arad - Prof. Dan Schwarzfuchs" by "dj_deep"
       - starRating 2, memberSince "15.1.24", postCount 1200
       - 6 replies, max depth 2

    4. **Export**: `ThreadData`, `ThreadPost`, `ReplyTreeItem` types and `THREAD_SEED` array

    Create `src/__tests__/thread-seed.test.ts` to validate:
    - THREAD_SEED has at least 2 entries
    - Thread 940165 has 35+ replies
    - Thread 940165 has replies at depth >= 3
    - Every reply has valid depth >= 1
    - starRating is 1-5 for all posts
    - All required ThreadPost fields are present and non-empty
  </action>
  <verify>
    <automated>npx jest --testPathPattern="thread-seed" --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - ThreadData, ThreadPost, ReplyTreeItem interfaces exported
    - THREAD_SEED has 2 threads: 940165 (35 replies, depth 6) and 940099 (6 replies, depth 2)
    - thread-seed.test.ts passes with all assertions green
  </acceptance_criteria>
  <done>THREAD_SEED exported with 2 threads, types are importable, seed test passes</done>
</task>

<task type="auto">
  <name>Task 2: Create Wave 0 test stubs for all thread components</name>
  <files>src/__tests__/ThreadBreadcrumb.test.tsx, src/__tests__/OriginalPostBlock.test.tsx, src/__tests__/ActionButtons.test.tsx, src/__tests__/ReplyTree.test.tsx, src/__tests__/QuickReplyForm.test.tsx, src/__tests__/ThreadPageClient.test.tsx</files>
  <read_first>
    - .planning/phases/04-thread-page/04-RESEARCH.md (Validation Architecture section — test map and patterns)
    - src/__tests__/ThreadRow.test.tsx (existing test pattern in project)
    - src/__tests__/ForumThreadTable.test.tsx (existing test factory pattern)
  </read_first>
  <action>
    Create 6 test files with failing tests. Each test imports a component that does not exist yet — tests MUST fail with "Cannot find module" or similar import error. Use the makeReplyItem() / makeThreadPost() factory pattern from RESEARCH.md.

    **src/__tests__/ThreadBreadcrumb.test.tsx** (THRD-08):
    - Test: renders "Forums" link with href="/"
    - Test: renders section name link with href="/forum/scoops1"
    - Test: renders "Thread #940165" text (not a link)
    - Test: breadcrumb bar has backgroundColor matching --rotter-subheader-blue
    - Import: `import { ThreadBreadcrumb } from "@/components/thread/ThreadBreadcrumb";`

    **src/__tests__/OriginalPostBlock.test.tsx** (THRD-01, THRD-02):
    - Create `makeThreadPost()` factory returning a valid ThreadPost
    - Test THRD-01: renders author name "mortzix" in bold
    - Test THRD-01: renders star rating image with src="/icons/star-3.svg"
    - Test THRD-01: renders "Member since 1.3.26"
    - Test THRD-01: renders "6555 posts"
    - Test THRD-01: renders raters count and points
    - Test THRD-02: renders h1 with post title
    - Test THRD-02: renders 70%-width nested table (check style attribute contains "70%")
    - Import: `import { OriginalPostBlock } from "@/components/thread/OriginalPostBlock";`

    **src/__tests__/ActionButtons.test.tsx** (THRD-03):
    - Test: renders 5 action links: "Edit", "Up", "Reply", "View All", "Back"
    - Test: "Reply" button calls onReplyClick callback when clicked
    - Test: "Back" link has href pointing to forum listing
    - Import: `import { ActionButtons } from "@/components/thread/ActionButtons";`

    **src/__tests__/ReplyTree.test.tsx** (THRD-04, THRD-05, THRD-06):
    - Create `makeReplyItem()` factory returning a valid ReplyTreeItem
    - Test THRD-04: renders 4 column headers: "Thread", "Author", "Date", "#"
    - Test THRD-05: top-level reply (depth=1) uses message.svg icon
    - Test THRD-05: nested reply (depth=2) uses reply-message.svg icon
    - Test THRD-05: depth=3 reply has 8 nbsp characters before icon (verify textContent includes 8x \u00a0)
    - Test THRD-06: first reply row uses --rotter-row-even, second uses --rotter-row-odd
    - Import: `import { ReplyTree } from "@/components/thread/ReplyTree";`

    **src/__tests__/QuickReplyForm.test.tsx** (THRD-07):
    - Test: form is not rendered when visible=false
    - Test: form renders textarea and submit button when visible=true
    - Test: submit button text is "Submit Reply"
    - Import: `import { QuickReplyForm } from "@/components/thread/QuickReplyForm";`

    **src/__tests__/ThreadPageClient.test.tsx** (THRD-07 toggle integration):
    - Import: `import { ThreadPageClient } from "@/components/thread/ThreadPageClient";`
    - Import ThreadData type and create a minimal makeThreadData() factory with valid post and 2 replies
    - test.todo: "QuickReplyForm is not rendered by default"
    - test.todo: "clicking Reply button renders QuickReplyForm"
    - These are test.todo stubs (not full assertions) — Plan 04-04 will fill in the real assertions after ThreadPageClient is implemented

    All tests use `@testing-library/react` render + screen. All tests will fail (RED) because components do not exist yet.
  </action>
  <verify>
    <automated>npx jest --testPathPattern="ThreadBreadcrumb|OriginalPostBlock|ActionButtons|ReplyTree|QuickReplyForm|ThreadPageClient" --no-coverage 2>&1 | grep -E "(FAIL|PASS|Tests:)" | head -12</automated>
  </verify>
  <acceptance_criteria>
    - All 6 test files exist
    - Running them produces FAIL (module not found) for all 6
    - Test count: at least 20 total test cases across 6 files (ThreadPageClient has 2 test.todo)
  </acceptance_criteria>
  <done>6 test files exist and fail with import errors (Wave 0 RED state achieved)</done>
</task>

</tasks>

<verification>
- `npx jest --testPathPattern="thread-seed" --no-coverage` passes (seed data validated)
- `npx jest --testPathPattern="ThreadBreadcrumb|OriginalPostBlock|ActionButtons|ReplyTree|QuickReplyForm|ThreadPageClient" --no-coverage` fails (expected — components not yet created)
- TypeScript compiles: `npx tsc --noEmit --project tsconfig.json` (thread-seed.ts has no type errors)
</verification>

<success_criteria>
- ThreadData, ThreadPost, ReplyTreeItem types exported from thread-seed.ts
- THREAD_SEED has 2 threads with correct structure
- 6 component test files exist with 20+ failing tests covering all 8 THRD requirements
</success_criteria>

<output>
After completion, create `.planning/phases/04-thread-page/04-01-SUMMARY.md`
</output>
