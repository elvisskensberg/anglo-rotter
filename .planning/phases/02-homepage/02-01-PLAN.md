---
phase: 02-homepage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/homepage-seed.ts
  - src/__tests__/HomepageLayout.test.tsx
  - src/__tests__/BreakingNewsFeed.test.tsx
  - src/__tests__/TickerSidebar.test.tsx
  - src/__tests__/AdSlot.test.tsx
  - src/components/homepage/BreakingNewsFeed.tsx
  - src/components/homepage/BreakingNewsFeed.module.css
  - src/components/homepage/TickerSidebar.tsx
  - src/components/homepage/TickerSidebar.module.css
  - src/components/homepage/AdSlot.tsx
  - src/components/homepage/index.ts
autonomous: true
requirements: [HOME-01, HOME-02, HOME-03, HOME-05]

must_haves:
  truths:
    - "Breaking news feed renders 20 headlines with red timestamps and navy headline text"
    - "Ticker sidebar renders a 300x430px scrollable div with overflow-y:scroll"
    - "Ad placeholder slots render with correct pixel dimensions and gray borders"
  artifacts:
    - path: "src/data/homepage-seed.ts"
      provides: "Typed seed data (20 headlines from snapshots)"
      exports: ["HomepageSeedItem", "HOMEPAGE_SEED", "TICKER_SEED"]
    - path: "src/components/homepage/BreakingNewsFeed.tsx"
      provides: "Center column breaking news feed"
      exports: ["BreakingNewsFeed"]
    - path: "src/components/homepage/TickerSidebar.tsx"
      provides: "Left sidebar scrolling ticker"
      exports: ["TickerSidebar"]
    - path: "src/components/homepage/AdSlot.tsx"
      provides: "Reusable ad placeholder component"
      exports: ["AdSlot"]
    - path: "src/components/homepage/index.ts"
      provides: "Barrel export for homepage components"
  key_links:
    - from: "src/components/homepage/BreakingNewsFeed.tsx"
      to: "src/data/homepage-seed.ts"
      via: "imports HomepageSeedItem type"
      pattern: "import.*HomepageSeedItem.*from.*homepage-seed"
    - from: "src/components/homepage/TickerSidebar.tsx"
      to: "src/data/homepage-seed.ts"
      via: "imports TICKER_SEED data"
      pattern: "import.*TICKER_SEED.*from.*homepage-seed"
---

<objective>
Create the homepage seed data, test stubs, and leaf components (BreakingNewsFeed, TickerSidebar, AdSlot) that will be composed into the 3-column layout in Plan 02.

Purpose: Establish the typed data contract and individually testable components before wiring them into the page layout.
Output: Seed data file, 4 test files, 3 components with CSS Modules, barrel export.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@data/design/DESIGN_SPECIFICATION.md (Section 2: Homepage)
@data/design/homepage.html (raw Rotter homepage HTML)
@src/app/globals.css (CSS tokens)
@src/components/ui/Table.tsx (Table component API)
@.planning/phases/02-homepage/02-RESEARCH.md

<interfaces>
<!-- From src/components/ui/Table.tsx -->
```typescript
interface TableProps extends React.TableHTMLAttributes<HTMLTableElement> {
  children: React.ReactNode;
}
export function Table({ children, className, ...props }: TableProps)
```

<!-- From src/app/globals.css — relevant tokens -->
```css
--rotter-text-primary: #000099;
--rotter-text-time: red;
--rotter-sidebar: 300px;
--rotter-center-col: 450px;
--rotter-size-xs: 9pt;
--rotter-size-news-headline: 10pt;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0a: Seed data and HomepageLayout test stub</name>
  <files>src/data/homepage-seed.ts, src/__tests__/HomepageLayout.test.tsx</files>
  <read_first>data/snapshots/scoops1_2026-03-22.json, src/__tests__/Table.test.tsx, src/app/globals.css</read_first>
  <action>
1. Create `src/data/homepage-seed.ts` with:
   - `HomepageSeedItem` interface: `{ time: string; headline: string; url: string }`
   - `TickerSeedItem` interface: `{ date: string; text: string; category: string; url: string }`
   - `HOMEPAGE_SEED` const array of 20 items, extracted from `data/snapshots/scoops1_2026-03-22.json` using `title_en` field for headline and time portion of `date` field (extract HH:MM after space, or use "00:00" if no time). Convert URLs to local paths like `/forum/scoops1/{id}`.
   - `TICKER_SEED` const array of 10 placeholder ticker items with dates like "03/22 18:42", text from translated headlines, categories like "Security", "Politics", and urls matching the seed pattern.

2. Create `src/__tests__/HomepageLayout.test.tsx` (per HOME-01) following the pattern in `src/__tests__/Table.test.tsx`:
   - Test: "renders 3-column table with correct widths" — render HomepageLayout, assert table exists, assert 3 td elements, check left td has `width: 300px`, center td has `width: 450px`
   - Test: "left td has bgcolor white" — check `backgroundColor: #ffffff` style
   - Test: "outer table has width 1012px and table-layout fixed" — check style attributes

   Import from `@testing-library/jest-dom` and `@testing-library/react`. Use `@/components/homepage` import path for HomepageLayout.
  </action>
  <verify>
    <automated>pnpm test -- --testPathPattern="HomepageLayout" 2>&1 | grep -E "(PASS|FAIL|Cannot find|SyntaxError)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/data/homepage-seed.ts exports HomepageSeedItem, TickerSeedItem, HOMEPAGE_SEED (20 items), TICKER_SEED (10 items)
    - HomepageLayout test stub exists in src/__tests__/ with 3 descriptive test names
    - Test initially FAILs (red) because HomepageLayout component does not exist yet
  </acceptance_criteria>
  <done>Seed data file has 20 typed headlines and 10 ticker items. HomepageLayout test stub compiles and attempts to run (failure expected since component is not yet built).</done>
</task>

<task type="auto">
  <name>Task 0b: Component test stubs (BreakingNewsFeed, TickerSidebar, AdSlot)</name>
  <files>src/__tests__/BreakingNewsFeed.test.tsx, src/__tests__/TickerSidebar.test.tsx, src/__tests__/AdSlot.test.tsx</files>
  <read_first>src/__tests__/Table.test.tsx, src/data/homepage-seed.ts</read_first>
  <action>
Create 3 test stub files following the pattern in `src/__tests__/Table.test.tsx`:

1. `src/__tests__/BreakingNewsFeed.test.tsx`:
   - Test: "renders all seed headlines" — pass HOMEPAGE_SEED, assert 20 headline texts present
   - Test: "time labels have red color via CSS module class" — assert `.timeLabel` class on time spans
   - Test: "headline links have navy color via CSS module class" — assert `.headlineLink` class on anchor elements
   - Test: "headline links have target=news" — assert `target="news"` attribute

2. `src/__tests__/TickerSidebar.test.tsx`:
   - Test: "renders scrollable div with correct dimensions" — assert outer div has `width: 300px`, `height: 430px`, `overflow-y: scroll`
   - Test: "renders ticker items with diegoTitle class" — assert `.diegoTitle` class on date spans
   - Test: "renders ticker items with diegoContent class" — assert `.diegoContent` class on link elements

3. `src/__tests__/AdSlot.test.tsx`:
   - Test: "renders with correct width and height" — pass width=300, height=250, label="Test", assert div has `width: 300px`, `height: 250px`
   - Test: "displays label text" — assert "Ad: Test (300x250)" text present
   - Test: "has gray border" — assert `border: 1px solid #cccccc` style

All test files import from `@testing-library/jest-dom` and `@testing-library/react`. Use `@/components/homepage` import path for components and `@/data/homepage-seed` for seed data.
  </action>
  <verify>
    <automated>pnpm test -- --testPathPattern="(BreakingNews|Ticker|AdSlot)" 2>&1 | grep -E "(PASS|FAIL|Cannot find|SyntaxError)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - 3 test files exist in src/__tests__/ with descriptive test names
    - Tests initially FAIL (red) because components do not exist yet
  </acceptance_criteria>
  <done>Three component test stub files compile and attempt to run (failures expected since components are not yet built).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1a: BreakingNewsFeed component</name>
  <files>src/components/homepage/BreakingNewsFeed.tsx, src/components/homepage/BreakingNewsFeed.module.css</files>
  <read_first>src/__tests__/BreakingNewsFeed.test.tsx, src/data/homepage-seed.ts, data/design/DESIGN_SPECIFICATION.md (section 2), src/app/globals.css</read_first>
  <behavior>
    - BreakingNewsFeed renders each headline as: span.timeLabel (9pt red bold) + nbsp + a.headlineLink with target="news" (10pt #000099) + br
  </behavior>
  <action>
1. Create `src/components/homepage/BreakingNewsFeed.tsx`:
   - Props: `{ headlines: HomepageSeedItem[] }`
   - Import `HomepageSeedItem` from `@/data/homepage-seed`
   - Render a "Breaking News" header bar: div with backgroundColor `var(--rotter-subheader-blue)` (#3293CD), white bold text "Breaking News", padding 2px 8px, fontSize 10pt
   - Below header, render each headline using React.Fragment with key={index}:
     - `<span className={styles.timeLabel}><b>{item.time}</b></span>`
     - `{"\u00a0"}`
     - `<a href={item.url} target="news" className={styles.headlineLink}>{item.headline}</a>`
     - `<br />`

2. Create `src/components/homepage/BreakingNewsFeed.module.css`:
   ```css
   .timeLabel {
     font-size: var(--rotter-size-xs);  /* 9pt */
     color: var(--rotter-text-time);     /* red */
     font-weight: bold;
   }
   .headlineLink {
     font-size: var(--rotter-size-news-headline);  /* 10pt */
     color: var(--rotter-text-primary);              /* #000099 */
     text-decoration: none;
   }
   .headlineLink:hover {
     text-decoration: underline;
   }
   ```
  </action>
  <verify>
    <automated>pnpm test -- --testPathPattern="BreakingNews" --bail</automated>
  </verify>
  <acceptance_criteria>
    - BreakingNewsFeed test: all 4 tests pass (renders headlines, timeLabel class, headlineLink class, target=news)
    - CSS module uses var(--rotter-*) tokens, not hardcoded hex values
  </acceptance_criteria>
  <done>BreakingNewsFeed renders red timestamps + navy headline links matching DESIGN_SPECIFICATION.md section 2. All 4 tests pass green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1b: TickerSidebar, AdSlot, and barrel export</name>
  <files>src/components/homepage/TickerSidebar.tsx, src/components/homepage/TickerSidebar.module.css, src/components/homepage/AdSlot.tsx, src/components/homepage/index.ts</files>
  <read_first>src/__tests__/TickerSidebar.test.tsx, src/__tests__/AdSlot.test.tsx, src/data/homepage-seed.ts, data/design/DESIGN_SPECIFICATION.md (section 2), src/app/globals.css</read_first>
  <behavior>
    - TickerSidebar renders a div with width:300px, height:430px, overflow-y:scroll, overflow-x:hidden, direction:ltr, text-align:left
    - TickerSidebar renders inner items with span.diegoTitle (10pt red bold) + a.diegoContent (8pt #000099)
    - AdSlot renders a div with exact width/height px, 1px solid #cccccc border, #f5f5f5 background, centered label text
  </behavior>
  <action>
1. Create `src/components/homepage/TickerSidebar.tsx`:
   - Import `TICKER_SEED` from `@/data/homepage-seed`
   - Render outer div with inline styles: `{ position: "relative", width: "300px", height: "430px", overflowY: "scroll", overflowX: "hidden", direction: "ltr", textAlign: "left" }`
   - Render inner div with style: `{ width: "260px", position: "absolute" }`
   - Map TICKER_SEED items:
     - `<span className={styles.diegoTitle}>{item.date}</span>`
     - `{"\u00a0\u00a0"}`
     - `<a className={styles.diegoContent} href={item.url} target="new">{item.text} <b>({item.category})</b></a>`
     - `<br /><br />`

2. Create `src/components/homepage/TickerSidebar.module.css`:
   ```css
   .diegoTitle {
     font-weight: bold;
     font-size: 10pt;
     color: var(--rotter-text-time);  /* red */
   }
   .diegoContent {
     color: var(--rotter-text-primary);  /* #000099 */
     font-size: 8pt;
     text-decoration: none;
   }
   .diegoContent:hover {
     text-decoration: underline;
   }
   ```

3. Create `src/components/homepage/AdSlot.tsx`:
   - Props: `{ width: number; height: number; label: string }`
   - Render a div with inline styles: `{ width: ${width}px, height: ${height}px, border: "1px solid #cccccc", backgroundColor: "#f5f5f5", display: "flex", alignItems: "center", justifyContent: "center", fontSize: "11px", color: "#999999", margin: "3px auto" }`
   - Inner text: `Ad: {label} ({width}x{height})`

4. Create `src/components/homepage/index.ts`:
   ```typescript
   export { BreakingNewsFeed } from "./BreakingNewsFeed";
   export { TickerSidebar } from "./TickerSidebar";
   export { AdSlot } from "./AdSlot";
   ```
  </action>
  <verify>
    <automated>pnpm test -- --testPathPattern="(Ticker|AdSlot)" --bail</automated>
  </verify>
  <acceptance_criteria>
    - TickerSidebar test: all 3 tests pass (dimensions, diegoTitle class, diegoContent class)
    - AdSlot test: all 3 tests pass (dimensions, label text, border)
    - CSS modules use var(--rotter-*) tokens, not hardcoded hex values
    - Barrel export at src/components/homepage/index.ts exports all 3 components
  </acceptance_criteria>
  <done>TickerSidebar and AdSlot render correctly with CSS Module styling. 6 component tests pass green. Barrel export wires all 3 components.</done>
</task>

</tasks>

<verification>
- `pnpm test -- --testPathPattern="(BreakingNews|Ticker|AdSlot)" --bail` — all 10 tests green
- `grep -r "var(--rotter-" src/components/homepage/*.module.css` — all colors use tokens
- `grep -c "export" src/components/homepage/index.ts` — returns 3 (BreakingNewsFeed, TickerSidebar, AdSlot)
</verification>

<success_criteria>
- 20 typed seed headlines in homepage-seed.ts sourced from real snapshot data
- BreakingNewsFeed renders red timestamps + navy headlines matching DESIGN_SPECIFICATION.md section 2
- TickerSidebar renders 300x430 scrollable div with correct overflow and direction
- AdSlot renders gray-bordered placeholder divs with correct pixel dimensions
- All tests pass, no hardcoded hex in components
</success_criteria>

<output>
After completion, create `.planning/phases/02-homepage/02-01-SUMMARY.md`
</output>
