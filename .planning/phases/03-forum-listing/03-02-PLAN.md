---
phase: 03-forum-listing
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/components/forum/ForumToolbar.tsx
  - src/components/forum/ForumToolbar.module.css
  - src/components/forum/PaginationBar.tsx
  - src/components/forum/PaginationBar.module.css
  - src/components/forum/ForumSectionDropdown.tsx
  - src/components/forum/ForumSectionDropdown.module.css
  - src/__tests__/ForumToolbar.test.tsx
  - src/__tests__/PaginationBar.test.tsx
autonomous: true
requirements:
  - FORUM-05
  - FORUM-06
  - FORUM-07

must_haves:
  truths:
    - "ForumToolbar renders 4 icons (Login, Help, Search, Post) each at 33x33px with labels below"
    - "PaginationBar shows red page numbers with current page unlinked"
    - "PaginationBar rows-per-page dropdown offers 15/30/50/100/150/200/250/300 with default 30"
    - "Changing rows-per-page resets to page 1"
    - "ForumSectionDropdown lists Scoops/Politics/Media/Economy/Sports/Culture"
  artifacts:
    - path: "src/components/forum/ForumToolbar.tsx"
      provides: "Toolbar with 4 action icons"
      exports: ["ForumToolbar"]
    - path: "src/components/forum/PaginationBar.tsx"
      provides: "Pagination with page numbers and rows-per-page"
      exports: ["PaginationBar"]
    - path: "src/components/forum/ForumSectionDropdown.tsx"
      provides: "Forum category selector dropdown"
      exports: ["ForumSectionDropdown"]
  key_links:
    - from: "src/components/forum/PaginationBar.tsx"
      to: "ForumThreadTable (plan 03)"
      via: "props: currentPage, totalPages, rowsPerPage, onPageChange, onRowsPerPageChange"
      pattern: "onPageChange|onRowsPerPageChange"
    - from: "src/components/forum/ForumSectionDropdown.tsx"
      to: "ForumThreadTable (plan 03)"
      via: "props: selectedCategory, onCategoryChange"
      pattern: "onCategoryChange"
---

<objective>
Build the three supporting components for the forum page: ForumToolbar (action icons), PaginationBar (page numbers + rows-per-page), and ForumSectionDropdown (category selector).

Purpose: These components are leaf-level widgets consumed by ForumThreadTable in Plan 03. Building them first means Plan 03 can focus on orchestration, tooltip state, and route wiring.

Output: 3 fully tested components with CSS Modules.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-forum-listing/03-CONTEXT.md
@.planning/phases/03-forum-listing/03-RESEARCH.md
@.planning/phases/03-forum-listing/03-01-SUMMARY.md
@src/app/globals.css

<interfaces>
<!-- ForumThread type from Plan 01 (src/data/forum-seed.ts) -->
export interface ForumThread {
  id: number;
  title: string;
  author: string;
  date: string;
  time: string;
  lastReplyDate: string;
  lastReplyTime: string;
  lastReplyAuthor: string;
  lastReplyNum: number;
  replyCount: number;
  viewCount: number;
  excerpt: string;
  category: 'scoops' | 'politics' | 'media' | 'economy' | 'sports' | 'culture';
  url: string;
}

<!-- PaginationBar props contract (consumed by ForumThreadTable in Plan 03) -->
interface PaginationBarProps {
  currentPage: number;
  totalPages: number;
  rowsPerPage: number;
  onPageChange: (page: number) => void;
  onRowsPerPageChange: (rows: number) => void;
}

<!-- ForumSectionDropdown props contract -->
interface ForumSectionDropdownProps {
  selectedCategory: string;
  onCategoryChange: (category: string) => void;
}

<!-- CSS tokens from globals.css -->
--rotter-text-primary: #000099
--rotter-icon-toolbar: 33px
--rotter-font-primary: Arial, Helvetica, sans-serif
--rotter-text-red: red

<!-- Available icons in public/icons/ -->
toolbar-login.svg, toolbar-help.svg, toolbar-search.svg, toolbar-post.svg
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ForumToolbar component with 4 action icons</name>
  <files>
    src/components/forum/ForumToolbar.tsx,
    src/components/forum/ForumToolbar.module.css,
    src/__tests__/ForumToolbar.test.tsx
  </files>
  <read_first>
    src/__tests__/ForumToolbar.test.tsx,
    src/components/layout/BlueNavBar.tsx,
    .planning/phases/03-forum-listing/03-RESEARCH.md
  </read_first>
  <behavior>
    - Test: Renders 4 img elements
    - Test: Each img has width=33 and height=33
    - Test: Icons are toolbar-login.svg, toolbar-help.svg, toolbar-search.svg, toolbar-post.svg
    - Test: Labels "Login", "Help", "Search", "Post" appear as text
    - Test: Each icon is inside a table cell with align="center"
  </behavior>
  <action>
    Replace the test.todo stubs in `src/__tests__/ForumToolbar.test.tsx` with real tests (TDD red phase first).

    Create `src/components/forum/ForumToolbar.tsx`:
    - Stateless component, no props needed
    - Renders a `<table>` with one `<tbody><tr>` containing 4 cells
    - Each cell: align="center", nowrap, width=50
    - Each cell contains: `<a>` wrapping `<img>` (33x33) + `<br/>` + label text
    - Icons: /icons/toolbar-login.svg, /icons/toolbar-help.svg, /icons/toolbar-search.svg, /icons/toolbar-post.svg
    - Labels: "Login", "Help", "Search", "Post" in font size -1, Arial, color #000099
    - Links: /login, /help, /search, /post (placeholder hrefs)

    Per research Pattern 7 and code example (line 439 of scoops_forum.html).

    CSS Module: minimal — label text styling with var(--rotter-text-primary) and var(--rotter-font-primary).
  </action>
  <verify>
    <automated>pnpm test -- --testPathPattern=ForumToolbar</automated>
  </verify>
  <acceptance_criteria>
    - 4 icons render at 33x33
    - Labels visible below each icon
    - Table-based layout matching Rotter structure
    - All ForumToolbar tests pass
  </acceptance_criteria>
  <done>ForumToolbar renders 4 action icons at 33x33 with labels; tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PaginationBar with page numbers and rows-per-page dropdown</name>
  <files>
    src/components/forum/PaginationBar.tsx,
    src/components/forum/PaginationBar.module.css,
    src/__tests__/PaginationBar.test.tsx
  </files>
  <read_first>
    src/__tests__/PaginationBar.test.tsx,
    .planning/phases/03-forum-listing/03-RESEARCH.md
  </read_first>
  <behavior>
    - Test: Renders correct number of page links (e.g., 60 threads / 30 per page = 2 pages)
    - Test: Current page is rendered as plain text (not a link)
    - Test: Other pages rendered as red bold text inside links
    - Test: Page separator "|" between page numbers
    - Test: Rows-per-page select has options 15, 30, 50, 100, 150, 200, 250, 300
    - Test: Default selected value matches rowsPerPage prop
    - Test: Clicking a page number calls onPageChange with that number
    - Test: Changing select value calls onRowsPerPageChange with new value
  </behavior>
  <action>
    Replace test.todo stubs in `src/__tests__/PaginationBar.test.tsx` with real tests (TDD red phase first).

    Create `src/components/forum/PaginationBar.tsx`:
    - Props: currentPage, totalPages, rowsPerPage, onPageChange, onRowsPerPageChange
    - Render page numbers 1 through totalPages:
      - Current page: plain text (no link)
      - Other pages: `<a>` with red bold text, onClick calls onPageChange(pageNum)
      - Separator: " | " between each number
    - Render rows-per-page section:
      - Label text "Show:" or similar
      - `<select>` with value={rowsPerPage}, onChange calls onRowsPerPageChange(Number(e.target.value))
      - Options: [15, 30, 50, 100, 150, 200, 250, 300]
      - Style: fontFamily Arial, fontSize 13px, color #000099, fontWeight bold

    Per research Pattern 6 and code examples (line 548-562 of scoops_forum.html).

    CSS Module: page number styling (red, bold), current page styling (plain), layout alignment.
  </action>
  <verify>
    <automated>pnpm test -- --testPathPattern=PaginationBar</automated>
  </verify>
  <acceptance_criteria>
    - Page numbers render with correct count
    - Current page not linked, others are red bold links
    - Rows-per-page dropdown has all 8 options
    - Callbacks fire correctly on click/change
    - All PaginationBar tests pass
  </acceptance_criteria>
  <done>PaginationBar renders red page numbers + rows-per-page dropdown; tests pass</done>
</task>

<task type="auto">
  <name>Task 3: ForumSectionDropdown for category filtering</name>
  <files>
    src/components/forum/ForumSectionDropdown.tsx,
    src/components/forum/ForumSectionDropdown.module.css
  </files>
  <read_first>
    src/components/layout/DropdownMenu.tsx,
    .planning/phases/03-forum-listing/03-RESEARCH.md
  </read_first>
  <action>
    Create `src/components/forum/ForumSectionDropdown.tsx`:
    - Props: selectedCategory (string), onCategoryChange ((category: string) => void)
    - Render a `<select>` element with 6 options:
      - value="scoops" label="Scoops" (default)
      - value="politics" label="Politics"
      - value="media" label="Media"
      - value="economy" label="Economy"
      - value="sports" label="Sports"
      - value="culture" label="Culture"
    - onChange calls onCategoryChange with selected value
    - Style: fontFamily Arial, fontSize 13px, color #000099, fontWeight bold (matching Rotter forum select at lines 280-293 of scoops_forum.html)

    CSS Module: dropdown styling consistent with nav area appearance.

    Note: Per CONTEXT.md locked decision — "Switching sections filters seed data by category" (client-side filter, not navigation). Per research Pitfall 4 — do NOT navigate on section change.
  </action>
  <verify>
    <automated>pnpm test -- --testPathPattern=forum --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - Dropdown renders 6 category options
    - Selected value matches selectedCategory prop
    - onChange fires onCategoryChange callback
  </acceptance_criteria>
  <done>ForumSectionDropdown renders 6 forum categories with controlled select; component exists and compiles</done>
</task>

</tasks>

<verification>
pnpm test -- --testPathPattern="(ForumToolbar|PaginationBar)" — all tests pass
All 3 components export correctly from their files
Components use var(--rotter-*) tokens, no hardcoded hex values
</verification>

<success_criteria>
- ForumToolbar renders 4 icons at 33x33 with text labels
- PaginationBar shows red page numbers, rows-per-page with 8 options, default 30
- ForumSectionDropdown lists 6 forum categories
- All tests pass green
</success_criteria>

<output>
After completion, create `.planning/phases/03-forum-listing/03-02-SUMMARY.md`
</output>
