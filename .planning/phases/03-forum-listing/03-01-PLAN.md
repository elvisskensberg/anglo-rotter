---
phase: 03-forum-listing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/forum-seed.ts
  - src/__tests__/ThreadRow.test.tsx
  - src/__tests__/ForumToolbar.test.tsx
  - src/__tests__/PaginationBar.test.tsx
  - src/__tests__/ForumThreadTable.test.tsx
  - src/components/forum/ThreadRow.tsx
  - src/components/forum/ThreadRow.module.css
autonomous: true
requirements:
  - FORUM-01
  - FORUM-02
  - FORUM-03

must_haves:
  truths:
    - "ForumThread interface defines all 14 fields (id, title, author, date, time, lastReplyDate, lastReplyTime, lastReplyAuthor, lastReplyNum, replyCount, viewCount, excerpt, category, url)"
    - "FORUM_SEED array contains 60+ typed threads with deterministic view/reply counts showing normal, hot, and fire states"
    - "ThreadRow renders 6 table cells in correct order (icon, title 55%, author+date, last reply, replies, views)"
    - "ThreadRow applies alternating row background colors via isEven prop"
    - "ThreadRow renders thread-hot.svg when viewCount > 1000 or replyCount > 50, thread-fire.svg when viewCount > 5000"
    - "View count text renders red for hot/fire threads, orange (#ff9933) for normal"
  artifacts:
    - path: "src/data/forum-seed.ts"
      provides: "ForumThread interface + 60+ seed threads"
      exports: ["ForumThread", "FORUM_SEED"]
    - path: "src/components/forum/ThreadRow.tsx"
      provides: "Single thread row with 6 columns, hot indicators"
      exports: ["ThreadRow"]
    - path: "src/__tests__/ThreadRow.test.tsx"
      provides: "Tests for FORUM-01, FORUM-02, FORUM-03"
    - path: "src/__tests__/ForumToolbar.test.tsx"
      provides: "Test stub for FORUM-06"
    - path: "src/__tests__/PaginationBar.test.tsx"
      provides: "Test stub for FORUM-05"
    - path: "src/__tests__/ForumThreadTable.test.tsx"
      provides: "Test stub for FORUM-04, FORUM-07"
  key_links:
    - from: "src/components/forum/ThreadRow.tsx"
      to: "src/data/forum-seed.ts"
      via: "import type { ForumThread }"
      pattern: "import.*ForumThread.*forum-seed"
---

<objective>
Create the ForumThread data interface, seed data with 60+ threads, Wave 0 test stubs for all forum components, and the ThreadRow component with hot thread indicators.

Purpose: Establishes the data contract (ForumThread) that all subsequent plans depend on, provides test infrastructure, and delivers the core row component that renders the 6-column thread structure with hot/fire visual states.

Output: ForumThread type, FORUM_SEED array, 4 test files (stubs for future components + full tests for ThreadRow), ThreadRow component with CSS Module.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-forum-listing/03-CONTEXT.md
@.planning/phases/03-forum-listing/03-RESEARCH.md
@src/app/globals.css
@src/data/homepage-seed.ts
@src/components/ui/Table.tsx

<interfaces>
<!-- Existing CSS tokens from globals.css that ThreadRow must use -->
--rotter-row-odd: #FDFDFD
--rotter-row-even: #eeeeee
--rotter-views-orange: #ff9933
--rotter-views-hot: red
--rotter-text-primary: #000099
--rotter-lastby-link: #0000FF
--rotter-icon-sm: 16px
--rotter-size-thread-title: 15px
--rotter-font-primary: Arial, Helvetica, sans-serif

<!-- Existing icons in public/icons/ -->
thread-normal.svg, thread-hot.svg, thread-fire.svg, hot-news.svg
toolbar-login.svg, toolbar-help.svg, toolbar-search.svg, toolbar-post.svg
expand-threads.svg

<!-- Table component contract -->
From src/components/ui/Table.tsx:
export function Table({ children, className, ...props }: TableProps)
// Callers MUST include explicit <tbody>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ForumThread interface + seed data + Wave 0 test stubs</name>
  <files>
    src/data/forum-seed.ts,
    src/__tests__/ThreadRow.test.tsx,
    src/__tests__/ForumToolbar.test.tsx,
    src/__tests__/PaginationBar.test.tsx,
    src/__tests__/ForumThreadTable.test.tsx
  </files>
  <read_first>
    src/data/homepage-seed.ts,
    data/snapshots/scoops1_2026-03-22.json,
    src/__tests__/BreakingNewsFeed.test.tsx
  </read_first>
  <behavior>
    - Test: FORUM_SEED has length >= 60
    - Test: Every thread in FORUM_SEED satisfies ForumThread interface (has all 14 fields)
    - Test: At least 3 threads have viewCount > 5000 (fire state)
    - Test: At least 9 threads have viewCount > 1000 (hot state)
    - Test: At least 40 threads have viewCount <= 1000 (normal state)
  </behavior>
  <action>
    Create `src/data/forum-seed.ts` following the pattern from `src/data/homepage-seed.ts`:

    1. Define `ForumThread` interface with exactly these fields (per CONTEXT.md locked decision):
       - id: number
       - title: string
       - author: string
       - date: string (DD.MM.YY format)
       - time: string (HH:MM format, red display)
       - lastReplyDate: string
       - lastReplyTime: string
       - lastReplyAuthor: string
       - lastReplyNum: number
       - replyCount: number
       - viewCount: number
       - excerpt: string (first 200 chars for tooltip)
       - category: 'scoops' | 'politics' | 'media' | 'economy' | 'sports' | 'culture'
       - url: string

    2. Create `fabricateStats(threadId: number)` deterministic function:
       - threadId % 100 < 5 → viewCount 10000+ (fire), ~5% of threads
       - threadId % 100 < 20 → viewCount 2000-4000 (hot), ~15% of threads
       - Remaining → viewCount 100-3000 (normal), ~80% of threads
       - replyCount = Math.floor(viewCount / 80)

    3. Map the 60 threads from `data/snapshots/scoops1_2026-03-22.json`:
       - Use title_en for title
       - Use author field (fill "Anonymous" when null)
       - Parse date from snapshot date field into DD.MM.YY and HH:MM
       - Fabricate lastReply fields (use "User" + threadId % 20 for author)
       - Fabricate excerpt: first 200 chars of title repeated or padded
       - Map category from snapshot (security_alert → scoops, etc.)
       - Set url to `/forum/scoops1/{id}`
       - Add 10+ extra fabricated threads for politics, media, economy, sports, culture categories to reach 60+ total

    4. Export as `FORUM_SEED: ForumThread[]`

    5. Create Wave 0 test stubs:
       - `src/__tests__/ForumToolbar.test.tsx`: describe block with `test.todo` for "renders 4 icons at 33x33", "renders Login/Help/Search/Post labels"
       - `src/__tests__/PaginationBar.test.tsx`: describe block with `test.todo` for "renders correct page count", "rows-per-page change resets to page 1", "default rows-per-page is 30"
       - `src/__tests__/ForumThreadTable.test.tsx`: describe block with `test.todo` for "renders 6-column header row", "tooltip hidden by default", "tooltip visible on icon mouseenter", "section dropdown filters threads"
       - `src/__tests__/ThreadRow.test.tsx`: Full test file (not stub) — will contain real tests written in Task 2 TDD cycle
  </action>
  <verify>
    <automated>pnpm test -- --testPathPattern="(forum-seed|ForumToolbar|PaginationBar|ForumThreadTable)" --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - ForumThread interface exported from forum-seed.ts with all 14 fields
    - FORUM_SEED array exported with 60+ threads
    - Deterministic stats: ~5% fire, ~15% hot, ~80% normal
    - 4 test files exist (3 stubs with todos, 1 ready for ThreadRow tests)
  </acceptance_criteria>
  <done>FORUM_SEED contains 60+ typed threads; 4 test files exist; seed data tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ThreadRow component with hot indicators and alternating colors</name>
  <files>
    src/components/forum/ThreadRow.tsx,
    src/components/forum/ThreadRow.module.css,
    src/__tests__/ThreadRow.test.tsx
  </files>
  <read_first>
    src/data/forum-seed.ts,
    src/components/homepage/BreakingNewsFeed.tsx,
    src/app/globals.css,
    .planning/phases/03-forum-listing/03-RESEARCH.md
  </read_first>
  <behavior>
    - Test: ThreadRow renders 6 td cells in a single tr
    - Test: Title cell has width="55%"
    - Test: Row background is var(--rotter-row-even) when isEven=true, var(--rotter-row-odd) when false
    - Test: Normal thread (viewCount=500) renders thread-normal.svg icon
    - Test: Hot thread (viewCount=2000) renders thread-hot.svg icon
    - Test: Fire thread (viewCount=6000) renders thread-fire.svg icon
    - Test: Hot thread view count renders with color red
    - Test: Normal thread view count renders with color #ff9933
    - Test: Fire thread shows hot-news.svg next to view count
    - Test: Icon cell calls onIconEnter on mouseenter with thread title and excerpt
    - Test: Icon cell calls onIconLeave on mouseleave
  </behavior>
  <action>
    Create `src/components/forum/ThreadRow.tsx` per research Pattern 3 (hot thread logic) and code examples:

    1. Define `getThreadState(thread)` helper returning 'normal' | 'hot' | 'fire':
       - viewCount > 5000 → 'fire'
       - viewCount > 1000 || replyCount > 50 → 'hot'
       - else → 'normal'

    2. Define `iconMap` object: normal → '/icons/thread-normal.svg', hot → '/icons/thread-hot.svg', fire → '/icons/thread-fire.svg'

    3. ThreadRow props:
       - thread: ForumThread
       - isEven: boolean
       - onIconEnter: (title: string, excerpt: string, x: number, y: number) => void
       - onIconLeave: () => void

    4. Render a `<tr>` with inline style backgroundColor using var(--rotter-row-even) or var(--rotter-row-odd) based on isEven prop. 6 cells exactly matching research code examples:
       - Col 1: icon cell, align="right", valign="top", img 16x16, mouseenter/mouseleave handlers passing e.clientX/Y
       - Col 2: title cell, align="right", valign="top", width="55%", bold link with thread.title, font-size var(--rotter-size-thread-title)
       - Col 3: author+date cell, align="center", valign="top", date in #000099, time in red, author as bold link
       - Col 4: last reply cell, align="center", valign="top", same date/time/author pattern
       - Col 5: reply count cell, align="center", valign="top", bold #000099 text
       - Col 6: views cell, align="right", valign="top", bold text colored red (hot/fire) or #ff9933 (normal), fire threads also show hot-news.svg 16x16

    5. Use CSS Module for text15bn class (font: 15px Arial bold navy) and text13 class (font: 13px Arial).

    6. Use lowercase `bgcolor` is NOT needed — use inline style `backgroundColor` with CSS var references per Phase 2 pattern.

    Write tests in `src/__tests__/ThreadRow.test.tsx` following TDD (red-green):
    - Import render, screen from @testing-library/react
    - Wrap ThreadRow in a `<table><tbody>` for valid DOM
    - Test all behaviors listed above
  </action>
  <verify>
    <automated>pnpm test -- --testPathPattern=ThreadRow</automated>
  </verify>
  <acceptance_criteria>
    - ThreadRow renders exactly 6 td cells within a tr
    - Alternating colors applied via isEven prop using CSS vars
    - Hot/fire/normal icons selected by viewCount/replyCount thresholds
    - View count color is red for hot/fire, #ff9933 for normal
    - Fire threads show hot-news.svg secondary icon
    - onIconEnter/onIconLeave callbacks fire on mouseenter/mouseleave
    - All ThreadRow tests pass
  </acceptance_criteria>
  <done>ThreadRow component renders 6-column rows with correct hot indicators; all tests pass</done>
</task>

</tasks>

<verification>
pnpm test -- --testPathPattern="(ThreadRow|forum-seed)" — all tests pass
FORUM_SEED has 60+ entries with correct type
ThreadRow renders all 3 icon variants based on thresholds
</verification>

<success_criteria>
- ForumThread interface is the single source of truth for thread data shape
- FORUM_SEED provides 60+ threads with ~5% fire, ~15% hot, ~80% normal distribution
- ThreadRow renders pixel-accurate 6-column rows matching Rotter HTML structure
- Wave 0 test stubs exist for ForumToolbar, PaginationBar, ForumThreadTable
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/03-forum-listing/03-01-SUMMARY.md`
</output>
