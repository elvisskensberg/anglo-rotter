---
phase: 01-foundation-and-design-system
plan: 04
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - src/components/layout/HeaderBar.tsx
  - src/components/layout/HeaderBar.module.css
  - src/components/layout/BlueNavBar.tsx
  - src/components/layout/BlueNavBar.module.css
  - src/components/layout/OrangeNavBar.tsx
  - src/components/layout/OrangeNavBar.module.css
  - src/components/layout/DropdownMenu.tsx
  - src/components/layout/DropdownMenu.module.css
  - src/components/layout/index.ts
  - src/app/page.tsx
  - src/__tests__/HeaderBar.test.tsx
  - src/__tests__/NavBar.test.tsx
  - src/__tests__/Dropdown.test.tsx
autonomous: true
requirements: [LYOT-01, LYOT-02, LYOT-03, LYOT-04]

must_haves:
  truths:
    - "HeaderBar renders with logo, date, and gradient background at 1012px centered width"
    - "Blue navigation bar renders at 25px height with text-based nav buttons"
    - "Orange navigation bar renders at 24px height with text-based nav buttons"
    - "Dropdown menus appear on hover with #c6e0fb background and #D9D9D9 items"
    - "All layout components use table elements with explicit tbody tags"
  artifacts:
    - path: "src/components/layout/HeaderBar.tsx"
      provides: "Logo + date + search header component"
      exports: ["HeaderBar"]
    - path: "src/components/layout/BlueNavBar.tsx"
      provides: "25px blue navigation bar"
      exports: ["BlueNavBar"]
    - path: "src/components/layout/OrangeNavBar.tsx"
      provides: "24px orange navigation bar"
      exports: ["OrangeNavBar"]
    - path: "src/components/layout/DropdownMenu.tsx"
      provides: "CSS hover dropdown menu"
      exports: ["DropdownMenu"]
    - path: "src/components/layout/index.ts"
      provides: "Barrel export for all layout components"
      exports: ["HeaderBar", "BlueNavBar", "OrangeNavBar", "DropdownMenu"]
  key_links:
    - from: "src/components/layout/HeaderBar.module.css"
      to: "src/app/globals.css"
      via: "CSS custom properties"
      pattern: "var\\(--rotter-"
    - from: "src/components/layout/BlueNavBar.tsx"
      to: "src/components/layout/DropdownMenu.tsx"
      via: "Component composition"
      pattern: "DropdownMenu"
    - from: "src/app/page.tsx"
      to: "src/components/layout/index.ts"
      via: "Import barrel"
      pattern: "from.*@/components/layout"
---

<objective>
Build the four shared layout components (HeaderBar, BlueNavBar, OrangeNavBar, DropdownMenu) that form the visual shell used by every page in the application, wire them into the homepage to verify rendering, and fill in the stub test files with real assertions.

Purpose: Every page in Phases 2-6 imports these layout components. They define the consistent header and navigation structure that makes MultiRotter look like Rotter.net.
Output: Four layout components with CSS Modules, barrel export, homepage rendering the complete shell, and passing tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-design-system/01-RESEARCH.md
@data/design/DESIGN_SPECIFICATION.md

<interfaces>
<!-- From Plan 02 outputs — tokens and Table component -->

From src/app/globals.css (CSS custom properties):
  --rotter-header-blue: #71B7E6
  --rotter-subheader-blue: #3293CD
  --rotter-nav-blue: #2D8DCE
  --rotter-dropdown-bg: #c6e0fb
  --rotter-dropdown-item: #D9D9D9
  --rotter-dropdown-item-hover: #FFFFFF
  --rotter-orange-accent: #FF8400
  --rotter-text-header: #FFFFFF
  --rotter-text-primary: #000099
  --rotter-font-primary: Arial, Helvetica, sans-serif
  --rotter-size-menu: 16px
  --rotter-size-sm: 12px
  --rotter-container: 1012px
  --rotter-nav-bar-width: 975px
  --rotter-blue-bar-height: 25px
  --rotter-orange-bar-height: 24px

From src/components/ui/Table.tsx:
  export function Table({ children, className, ...props }: TableProps)
  // Wraps <table> with consistent defaults; callers MUST include <tbody>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DropdownMenu and navigation bar components</name>
  <files>src/components/layout/DropdownMenu.tsx, src/components/layout/DropdownMenu.module.css, src/components/layout/BlueNavBar.tsx, src/components/layout/BlueNavBar.module.css, src/components/layout/OrangeNavBar.tsx, src/components/layout/OrangeNavBar.module.css</files>
  <read_first>
    - src/app/globals.css (confirm CSS custom property names are available)
    - src/components/ui/Table.tsx (understand the Table component API)
    - data/design/DESIGN_SPECIFICATION.md (Section 2: Navigation Bars and Dropdown Menus)
    - .planning/phases/01-foundation-and-design-system/01-RESEARCH.md (Pattern 3: CSS Hover Dropdown, BlueNavBar component code example)
  </read_first>
  <action>
    Create `src/components/layout/` directory.

    **DropdownMenu component (LYOT-04):**

    Create `src/components/layout/DropdownMenu.tsx`:
    - Props: `{ label: string; items: { label: string; href: string }[] }`
    - Structure: A wrapper div containing a trigger span and a dropdown panel div
    - Dropdown panel is hidden by default, shown on `:hover` of the wrapper (pure CSS, no JS)
    - Export as named export: `export function DropdownMenu`

    Create `src/components/layout/DropdownMenu.module.css`:
    ```css
    .dropdownWrapper {
      position: relative;
      display: inline-block;
    }

    .dropdownTrigger {
      cursor: pointer;
      color: yellow;
      font-family: var(--rotter-font-primary);
      font-size: var(--rotter-size-menu);
      font-weight: bold;
      padding: 2px 10px;
    }

    .dropdownPanel {
      display: none;
      position: absolute;
      background: var(--rotter-dropdown-bg);
      z-index: 10;
      min-width: 150px;
      left: 0;
      top: 100%;
    }

    .dropdownWrapper:hover .dropdownPanel {
      display: block;
    }

    .dropdownItem {
      background-color: var(--rotter-dropdown-item);
      padding: 1px 30px 1px 15px;
      font: 10pt var(--rotter-font-primary);
      text-align: left;
    }

    .dropdownItem:hover {
      background-color: var(--rotter-dropdown-item-hover);
    }

    .dropdownItem a {
      color: var(--rotter-text-primary);
      text-decoration: none;
      display: block;
    }

    .dropdownItem a:hover {
      text-decoration: underline;
    }
    ```

    **BlueNavBar component (LYOT-02):**

    Create `src/components/layout/BlueNavBar.tsx`:
    - Height: 25px (var(--rotter-blue-bar-height))
    - Background: Uses a blue gradient or solid #2D8DCE (var(--rotter-nav-blue))
    - Width: 975px centered (var(--rotter-nav-bar-width))
    - Contains text-based navigation items (replacing original GIF buttons):
      `Archive | Exchange Rate | Opinion | Calendar | News Flashes | Weather | Home`
    - Each item is a link with white text, bold, 10pt
    - Items that have dropdowns render a `<DropdownMenu>` component instead of a plain link
    - Use a `<table>` with explicit `<tbody>` for the bar layout (per LYOT-05)

    Create `src/components/layout/BlueNavBar.module.css`:
    ```css
    .wrapper {
      display: flex;
      justify-content: center;
    }

    .navTable {
      width: var(--rotter-nav-bar-width);
      height: var(--rotter-blue-bar-height);
      background-color: var(--rotter-nav-blue);
    }

    .navItem {
      color: var(--rotter-text-header);
      font-family: var(--rotter-font-primary);
      font-weight: bold;
      font-size: 10pt;
      text-decoration: none;
      padding: 0 8px;
      white-space: nowrap;
    }

    .navItem:hover {
      text-decoration: underline;
    }
    ```

    **OrangeNavBar component (LYOT-03):**

    Create `src/components/layout/OrangeNavBar.tsx`:
    - Height: 24px (var(--rotter-orange-bar-height))
    - Background: #FF8400 (var(--rotter-orange-accent))
    - Width: 975px centered
    - Navigation items: `Scoops | Headlines | eNews | Index | Pro Business`
    - Same structure as BlueNavBar but with orange background
    - Use a `<table>` with explicit `<tbody>` (per LYOT-05)

    Create `src/components/layout/OrangeNavBar.module.css`:
    ```css
    .wrapper {
      display: flex;
      justify-content: center;
    }

    .navTable {
      width: var(--rotter-nav-bar-width);
      height: var(--rotter-orange-bar-height);
      background-color: var(--rotter-orange-accent);
    }

    .navItem {
      color: var(--rotter-text-header);
      font-family: var(--rotter-font-primary);
      font-weight: bold;
      font-size: 10pt;
      text-decoration: none;
      padding: 0 8px;
      white-space: nowrap;
    }

    .navItem:hover {
      text-decoration: underline;
    }
    ```

    All three components must:
    - Use CSS custom properties from globals.css (never hardcoded hex values)
    - Include explicit `<tbody>` in any `<table>` elements
    - Be exported as named exports

    After creating the components, update the stub test files with real assertions:

    **Update `src/__tests__/Dropdown.test.tsx`:**
    Replace todo tests with real render tests:
    ```typescript
    import "@testing-library/jest-dom";
    import { render, screen } from "@testing-library/react";
    import { DropdownMenu } from "@/components/layout/DropdownMenu";

    describe("DropdownMenu Component (LYOT-04)", () => {
      const items = [
        { label: "Item 1", href: "/item1" },
        { label: "Item 2", href: "/item2" },
      ];

      test("renders trigger label", () => {
        render(<DropdownMenu label="Test Menu" items={items} />);
        expect(screen.getByText("Test Menu")).toBeInTheDocument();
      });

      test("renders menu items as links", () => {
        render(<DropdownMenu label="Test Menu" items={items} />);
        expect(screen.getByText("Item 1")).toBeInTheDocument();
        expect(screen.getByText("Item 2")).toBeInTheDocument();
      });
    });
    ```

    **Update `src/__tests__/NavBar.test.tsx`:**
    Replace todo tests with real render tests:
    ```typescript
    import "@testing-library/jest-dom";
    import { render } from "@testing-library/react";
    import { BlueNavBar } from "@/components/layout/BlueNavBar";
    import { OrangeNavBar } from "@/components/layout/OrangeNavBar";

    describe("BlueNavBar Component (LYOT-02)", () => {
      test("renders navigation links", () => {
        const { container } = render(<BlueNavBar />);
        expect(container.querySelector("table")).toBeInTheDocument();
      });

      test("contains explicit tbody in table", () => {
        const { container } = render(<BlueNavBar />);
        expect(container.querySelector("tbody")).toBeInTheDocument();
      });
    });

    describe("OrangeNavBar Component (LYOT-03)", () => {
      test("renders navigation links", () => {
        const { container } = render(<OrangeNavBar />);
        expect(container.querySelector("table")).toBeInTheDocument();
      });

      test("contains explicit tbody in table", () => {
        const { container } = render(<OrangeNavBar />);
        expect(container.querySelector("tbody")).toBeInTheDocument();
      });
    });
    ```
  </action>
  <verify>
    <automated>grep -l "export function" src/components/layout/DropdownMenu.tsx src/components/layout/BlueNavBar.tsx src/components/layout/OrangeNavBar.tsx 2>/dev/null | wc -l && pnpm test -- --testPathPattern="(NavBar|Dropdown)" --bail 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/layout/DropdownMenu.tsx` exists and exports `DropdownMenu`
    - `src/components/layout/DropdownMenu.module.css` contains `var(--rotter-dropdown-bg)`
    - `src/components/layout/DropdownMenu.module.css` contains `.dropdownWrapper:hover .dropdownPanel` rule
    - `src/components/layout/BlueNavBar.tsx` exists and exports `BlueNavBar`
    - `src/components/layout/BlueNavBar.module.css` contains `var(--rotter-blue-bar-height)` or `25px`
    - `src/components/layout/OrangeNavBar.tsx` exists and exports `OrangeNavBar`
    - `src/components/layout/OrangeNavBar.module.css` contains `var(--rotter-orange-accent)` or `#FF8400`
    - All `.tsx` files that contain `<table` also contain `<tbody>`
    - `pnpm test -- --testPathPattern="(NavBar|Dropdown)" --bail` passes
  </acceptance_criteria>
  <done>DropdownMenu, BlueNavBar, and OrangeNavBar components exist with CSS Modules using design tokens, proper table/tbody structure, and passing tests</done>
</task>

<task type="auto">
  <name>Task 2: Create HeaderBar component, barrel export, wire into homepage, and finalize tests</name>
  <files>src/components/layout/HeaderBar.tsx, src/components/layout/HeaderBar.module.css, src/components/layout/index.ts, src/app/page.tsx, src/__tests__/HeaderBar.test.tsx</files>
  <read_first>
    - src/app/globals.css (confirm token names)
    - src/components/layout/BlueNavBar.tsx (confirm export name and props)
    - src/components/layout/OrangeNavBar.tsx (confirm export name and props)
    - data/design/DESIGN_SPECIFICATION.md (Section 2: Header Bar — 1012px, bg00.gif gradient, logo 335px, date white bold)
    - .planning/phases/01-foundation-and-design-system/01-RESEARCH.md (Pattern 5: HeaderBar with Gradient Background)
  </read_first>
  <action>
    **HeaderBar component (LYOT-01):**

    Create `src/components/layout/HeaderBar.tsx`:
    - Centered container at 1012px width (var(--rotter-container))
    - Background: CSS linear-gradient approximating `bg00.gif` — `linear-gradient(to right, #71B7E6, #3293CD, #71B7E6)`
    - Logo cell: 335px width, renders `<img src="/images/logo.svg" alt="MultiRotter" width={335} height={50} />`
    - Date cell: White bold text (var(--rotter-text-header)), font-size 12px (var(--rotter-size-sm)), displays current date
    - Uses `<table>` with explicit `<tbody>` for the header layout (per LYOT-05)
    - Export as `export function HeaderBar`

    Create `src/components/layout/HeaderBar.module.css`:
    ```css
    .headerOuter {
      display: flex;
      justify-content: center;
      width: 100%;
    }

    .headerTable {
      width: var(--rotter-container);
      background: linear-gradient(to right, var(--rotter-header-blue), var(--rotter-subheader-blue), var(--rotter-header-blue));
    }

    .logoCell {
      width: 335px;
      padding: 5px;
    }

    .dateCell {
      text-align: right;
      padding: 5px 10px;
      vertical-align: middle;
    }

    .dateText {
      color: var(--rotter-text-header);
      font-size: var(--rotter-size-sm);
      font-weight: bold;
      font-family: var(--rotter-font-primary);
    }
    ```

    **Barrel export:**

    Create `src/components/layout/index.ts`:
    ```typescript
    export { HeaderBar } from "./HeaderBar";
    export { BlueNavBar } from "./BlueNavBar";
    export { OrangeNavBar } from "./OrangeNavBar";
    export { DropdownMenu } from "./DropdownMenu";
    ```

    **Wire into homepage:**

    Update `src/app/page.tsx` to render the layout shell as a visual proof:
    ```tsx
    import { HeaderBar, BlueNavBar, OrangeNavBar } from "@/components/layout";

    export default function Home() {
      return (
        <div>
          <HeaderBar />
          <BlueNavBar />
          <OrangeNavBar />
          <main style={{ textAlign: "center", padding: "20px" }}>
            <p>MultiRotter — Foundation Shell</p>
          </main>
        </div>
      );
    }
    ```

    **Update `src/__tests__/HeaderBar.test.tsx`:**
    Replace todo tests with real render tests:
    ```typescript
    import "@testing-library/jest-dom";
    import { render, screen } from "@testing-library/react";
    import { HeaderBar } from "@/components/layout/HeaderBar";

    describe("HeaderBar Component (LYOT-01)", () => {
      test("renders logo image", () => {
        const { container } = render(<HeaderBar />);
        const img = container.querySelector("img[alt='MultiRotter']");
        expect(img).toBeInTheDocument();
      });

      test("contains explicit tbody in table", () => {
        const { container } = render(<HeaderBar />);
        expect(container.querySelector("tbody")).toBeInTheDocument();
      });

      test("renders a table element", () => {
        const { container } = render(<HeaderBar />);
        expect(container.querySelector("table")).toBeInTheDocument();
      });
    });
    ```

    Verify `pnpm build` and `pnpm test` both pass with all components in place.
  </action>
  <verify>
    <automated>pnpm test --bail 2>&1 | tail -8 && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/layout/HeaderBar.tsx` exists and exports `HeaderBar`
    - `src/components/layout/HeaderBar.module.css` contains `var(--rotter-container)` or `1012px`
    - `src/components/layout/HeaderBar.module.css` contains `linear-gradient`
    - `src/components/layout/index.ts` exports `HeaderBar`, `BlueNavBar`, `OrangeNavBar`, `DropdownMenu`
    - `src/app/page.tsx` imports from `@/components/layout`
    - `src/app/page.tsx` renders `<HeaderBar`
    - All `<table` elements in HeaderBar.tsx include `<tbody>`
    - `pnpm test --bail` passes (all test files green)
    - `pnpm build` exits with code 0
  </acceptance_criteria>
  <done>HeaderBar component renders with gradient background and logo at 1012px, all layout components are barrel-exported, homepage renders the complete header/nav shell, all tests pass, build passes</done>
</task>

</tasks>

<verification>
1. `pnpm test --bail` passes (all test files green)
2. `pnpm build` completes without errors
3. All 4 layout components exist: HeaderBar, BlueNavBar, OrangeNavBar, DropdownMenu
4. `grep -r "var(--rotter-" src/components/layout/*.module.css | wc -l` returns 5+ (tokens are used throughout)
5. `grep -r "<tbody>" src/components/layout/*.tsx | wc -l` matches the number of tables
6. `grep "from.*@/components/layout" src/app/page.tsx` succeeds
</verification>

<success_criteria>
- HeaderBar renders with logo, date text, and gradient background at 1012px centered
- BlueNavBar renders at 25px height with navigation links
- OrangeNavBar renders at 24px height with navigation links
- DropdownMenu shows on hover with correct #c6e0fb / #D9D9D9 colors
- All components use CSS custom properties (no hardcoded hex in TSX files)
- All table elements include explicit tbody
- Homepage renders the complete layout shell
- All tests pass (`pnpm test --bail`)
- Build passes (`pnpm build`)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-design-system/01-04-SUMMARY.md`
</output>
