---
phase: 01-foundation-and-design-system
plan: 03
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - public/icons/thread-normal.svg
  - public/icons/thread-hot.svg
  - public/icons/thread-fire.svg
  - public/icons/thread-camera.svg
  - public/icons/thread-new.svg
  - public/icons/hot-news.svg
  - public/icons/expand-threads.svg
  - public/icons/dir-arrow.svg
  - public/icons/toolbar-login.svg
  - public/icons/toolbar-help.svg
  - public/icons/toolbar-search.svg
  - public/icons/toolbar-post.svg
  - public/icons/message.svg
  - public/icons/reply-message.svg
  - public/icons/star-1.svg
  - public/icons/star-2.svg
  - public/icons/star-3.svg
  - public/icons/star-4.svg
  - public/icons/star-5.svg
  - public/images/logo.svg
autonomous: true
requirements: [DSGN-03]

must_haves:
  truths:
    - "Every SVG icon renders at the correct pixel size with image-rendering: pixelated applicable"
    - "Thread icons (normal, hot, fire, camera, new) exist as 16x16 SVGs"
    - "Toolbar icons (login, help, search, post) exist as 33x33 SVGs"
    - "Star rating icons (1-5) exist as SVGs"
    - "Reply tree icons (message, reply-message) exist as SVGs"
  artifacts:
    - path: "public/icons/thread-normal.svg"
      provides: "Normal thread icon"
      contains: "viewBox"
    - path: "public/icons/star-1.svg"
      provides: "1-star rating icon"
      contains: "viewBox"
    - path: "public/icons/toolbar-login.svg"
      provides: "Login toolbar icon"
      contains: "viewBox"
    - path: "public/icons/message.svg"
      provides: "Top-level reply tree icon"
      contains: "viewBox"
    - path: "public/images/logo.svg"
      provides: "Site logo placeholder"
      contains: "viewBox"
  key_links: []
---

<objective>
Create the complete SVG icon set that recreates all Rotter.net GIF icons as scalable vector graphics.

Purpose: All future phases (forum listing, thread page, news page, headlines) reference these icons. Without them, no page can render its thread indicators, toolbar, or reply tree.
Output: 19 SVG files in public/icons/ and 1 logo SVG in public/images/.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-design-system/01-RESEARCH.md
@data/design/DESIGN_SPECIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1a: Create thread status SVG icons</name>
  <files>public/icons/thread-normal.svg, public/icons/thread-hot.svg, public/icons/thread-fire.svg, public/icons/thread-camera.svg, public/icons/thread-new.svg</files>
  <read_first>
    - data/design/DESIGN_SPECIFICATION.md (Section 11: Icons and Images — thread icon descriptions, sizes)
  </read_first>
  <action>
    Create `public/icons/` directory if it does not exist.

    Create SVG files for the 5 thread status icons. Each SVG must:
    - Have a `viewBox="0 0 16 16"` attribute
    - Use simple geometric shapes that evoke the pixel-art character of the original GIFs
    - Use the Rotter color palette (reference DESIGN_SPECIFICATION.md Section 8)

    1. `thread-normal.svg` — Recreates `icon_general.gif`. A simple envelope/document icon in #000099 (dark navy). Represents a standard forum thread.
       - Design: 16x16 viewBox. Rectangle outline 2-12x3-13 with fold corner at top-right, filled #000099.

    2. `thread-hot.svg` — Recreates `hot_icon_general.gif`. Same as thread-normal but in red to indicate high view count.
       - Design: Same shape as thread-normal but fill color is `red`.

    3. `thread-fire.svg` — Recreates `new_icon_fire.gif` (16x16). A flame icon in red/orange for Red Alert threads.
       - Design: 16x16 viewBox. Simple flame shape using path, filled with gradient from #FF8400 (bottom) to red (top).

    4. `thread-camera.svg` — Recreates `new_icon_camera.gif` (16x16). A camera icon for threads with media.
       - Design: 16x16 viewBox. Simple camera body rectangle with lens circle, filled #000099.

    5. `thread-new.svg` — Recreates `new_icon_general.gif` (16x16). A document icon indicating new/unread thread.
       - Design: Same as thread-normal but with a small colored dot or star to indicate "new".
  </action>
  <verify>
    <automated>ls -1 public/icons/thread-*.svg 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - 5 SVG files exist: thread-normal.svg, thread-hot.svg, thread-fire.svg, thread-camera.svg, thread-new.svg
    - Each file contains `viewBox="0 0 16 16"`
    - Each file is valid XML (starts with `<svg`)
    - thread-normal uses #000099, thread-hot uses red, thread-fire uses gradient
  </acceptance_criteria>
  <done>All 5 thread status SVG icons exist in public/icons/ with 16x16 viewBox dimensions</done>
</task>

<task type="auto">
  <name>Task 1b: Create navigation SVG icons</name>
  <files>public/icons/hot-news.svg, public/icons/expand-threads.svg, public/icons/dir-arrow.svg</files>
  <read_first>
    - data/design/DESIGN_SPECIFICATION.md (Section 11: Icons and Images — navigation icon descriptions)
  </read_first>
  <action>
    Create SVG files for the 3 forum navigation icons. Each SVG must:
    - Have a `viewBox="0 0 16 16"` attribute
    - Use simple geometric shapes evoking the original pixel-art GIFs

    1. `hot-news.svg` — Recreates `hot_icon_news.gif`. Small indicator shown next to hot thread view counts.
       - Design: 16x16 viewBox. Small flame or exclamation in red.

    2. `expand-threads.svg` — Recreates `expand_threads.gif`. Toggle for threaded view.
       - Design: 16x16 viewBox. Plus/expand icon in #000099.

    3. `dir-arrow.svg` — Recreates `dir.gif`. Breadcrumb separator arrow.
       - Design: 16x16 viewBox. Right-pointing chevron in #000099.
  </action>
  <verify>
    <automated>ls -1 public/icons/hot-news.svg public/icons/expand-threads.svg public/icons/dir-arrow.svg 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - 3 SVG files exist: hot-news.svg, expand-threads.svg, dir-arrow.svg
    - Each file contains `viewBox` attribute
    - Each file is valid XML (starts with `<svg`)
  </acceptance_criteria>
  <done>All 3 navigation SVG icons exist in public/icons/ with 16x16 viewBox dimensions</done>
</task>

<task type="auto">
  <name>Task 2a: Create toolbar SVG icons</name>
  <files>public/icons/toolbar-login.svg, public/icons/toolbar-help.svg, public/icons/toolbar-search.svg, public/icons/toolbar-post.svg</files>
  <read_first>
    - data/design/DESIGN_SPECIFICATION.md (Section 11: Icons and Images — toolbar 33x33 icons)
  </read_first>
  <action>
    Create SVG files for the 4 toolbar icons. Per DESIGN_SPECIFICATION.md: `login.gif`, `help.gif`, `search.gif`, `post.gif` are all 33x33.

    1. `toolbar-login.svg` — Person/key icon in #000099 on light background. 33x33 viewBox.
    2. `toolbar-help.svg` — Question mark in circle, #000099. 33x33 viewBox.
    3. `toolbar-search.svg` — Magnifying glass, #000099. 33x33 viewBox.
    4. `toolbar-post.svg` — Pencil/pen on paper, #000099. 33x33 viewBox.

    Each SVG must have `viewBox="0 0 33 33"` and use simple geometric shapes.
  </action>
  <verify>
    <automated>ls -1 public/icons/toolbar-*.svg 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - 4 SVG files exist: toolbar-login.svg, toolbar-help.svg, toolbar-search.svg, toolbar-post.svg
    - Each file contains `viewBox="0 0 33 33"` (or equivalent)
    - Each file is valid XML (starts with `<svg`)
  </acceptance_criteria>
  <done>All 4 toolbar SVG icons exist in public/icons/ with 33x33 viewBox dimensions</done>
</task>

<task type="auto">
  <name>Task 2b: Create reply tree icons and logo SVG</name>
  <files>public/icons/message.svg, public/icons/reply-message.svg, public/images/logo.svg</files>
  <read_first>
    - data/design/DESIGN_SPECIFICATION.md (Section 11: Icons and Images — reply tree icons, logo)
  </read_first>
  <action>
    **Reply tree icons (16x16 viewBox):**

    1. `message.svg` — Recreates `message.gif`. Envelope icon for top-level replies. #000099.
    2. `reply-message.svg` — Recreates `reply_message.gif`. Smaller/indented envelope or curved arrow for nested replies. #3293CD to distinguish from top-level.

    **Logo:**

    3. `public/images/logo.svg` — Create `public/images/` directory if it does not exist. MultiRotter logo placeholder. Blue gradient text "MultiRotter" in a style evoking the Rotter.net branding. Use #71B7E6 and #3293CD. viewBox="0 0 335 50" (335px matches the original logo cell width from DESIGN_SPECIFICATION.md Section 2).
  </action>
  <verify>
    <automated>ls -1 public/icons/message.svg public/icons/reply-message.svg public/images/logo.svg 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `public/icons/message.svg` exists with viewBox
    - `public/icons/reply-message.svg` exists with viewBox
    - `public/images/logo.svg` exists with viewBox="0 0 335 50"
    - All files are valid XML (start with `<svg`)
  </acceptance_criteria>
  <done>Reply tree icons and logo SVG exist with correct viewBox dimensions</done>
</task>

<task type="auto">
  <name>Task 2c: Create star rating SVG icons</name>
  <files>public/icons/star-1.svg, public/icons/star-2.svg, public/icons/star-3.svg, public/icons/star-4.svg, public/icons/star-5.svg</files>
  <read_first>
    - data/design/DESIGN_SPECIFICATION.md (Section 11: Icons and Images — star ratings)
  </read_first>
  <action>
    Recreate `1_star.gif` through `5_star.gif`. Each shows N filled gold (#ff9933) stars out of 5. Stars that are not earned are shown as gray (#D9D9D9) outlines.

    1. `star-1.svg` — 1 filled star, 4 empty
    2. `star-2.svg` — 2 filled, 3 empty
    3. `star-3.svg` — 3 filled, 2 empty
    4. `star-4.svg` — 4 filled, 1 empty
    5. `star-5.svg` — 5 filled, 0 empty

    Each star SVG should use viewBox="0 0 80 16" (5 stars at 16px each = 80px wide) so they render as a horizontal row. Use a `<defs>` block with a reusable star `<symbol>` or `<path>` to keep the SVGs DRY.

    After creating all 5 star SVGs, run the icon tests to verify all icons from this plan:
    ```bash
    pnpm test -- --testPathPattern=icons --bail
    ```
  </action>
  <verify>
    <automated>ls -1 public/icons/star-*.svg 2>/dev/null | wc -l && pnpm test -- --testPathPattern=icons --bail 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - 5 SVG files exist: star-1.svg through star-5.svg
    - Each file contains `viewBox="0 0 80 16"`
    - star-1.svg has 1 filled (#ff9933) star and 4 empty (#D9D9D9)
    - star-5.svg has 5 filled stars and 0 empty
    - All files are valid XML (start with `<svg`)
    - `pnpm test -- --testPathPattern=icons` passes (all icons exist with viewBox)
  </acceptance_criteria>
  <done>All 5 star rating SVGs exist with correct fill counts, completing the full icon set (19 icons + 1 logo), verified by automated tests</done>
</task>

</tasks>

<verification>
1. `ls public/icons/*.svg | wc -l` returns 19 (all icon files)
2. `ls public/images/logo.svg` exists
3. `grep -l "viewBox" public/icons/*.svg | wc -l` matches the total SVG count
4. No GIF or PNG icon files exist in public/icons/ (all SVG)
5. `pnpm test -- --testPathPattern=icons --bail` passes
</verification>

<success_criteria>
- Complete SVG icon set covering: thread normal/hot/fire/camera/new, hot-news, expand-threads, dir-arrow, 4 toolbar icons, 2 reply tree icons, 5 star ratings
- Logo SVG placeholder in public/images/
- All SVGs have proper viewBox attributes for pixel-accurate rendering
- Icons use colors from the Rotter palette (#000099, #71B7E6, red, #ff9933, etc.)
- `pnpm test -- --testPathPattern=icons` passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-design-system/01-03-SUMMARY.md`
</output>
