---
phase: 08-pwa-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/manifest.ts
  - src/app/sw.ts
  - public/icons/icon-192x192.png
  - public/icons/icon-512x512.png
autonomous: true
requirements: [PWA-01, PWA-02]

must_haves:
  truths:
    - "Chrome DevTools Application > Manifest shows name MultiRotter, theme_color #71B7E6, icons at 192 and 512"
    - "Service worker registers with custom caching: NetworkFirst for /api/**, CacheFirst for /_next/static/** and image assets, StaleWhileRevalidate for navigations"
    - "DevTools Network tab shows SW-intercepted requests using the correct strategy per route"
  artifacts:
    - path: "src/app/manifest.ts"
      provides: "Web app manifest via Next.js metadata API"
      exports: ["default"]
    - path: "src/app/sw.ts"
      provides: "Custom Serwist caching strategies"
      contains: "NetworkFirst"
    - path: "public/icons/icon-192x192.png"
      provides: "192x192 PWA icon"
    - path: "public/icons/icon-512x512.png"
      provides: "512x512 maskable PWA icon"
  key_links:
    - from: "src/app/manifest.ts"
      to: "public/icons/icon-192x192.png"
      via: "icons array src path"
      pattern: "icon-192x192"
    - from: "src/app/sw.ts"
      to: "/api/**"
      via: "runtimeCaching urlPattern"
      pattern: "NetworkFirst"
---

<objective>
Create the web app manifest with proper metadata and icons, and replace the default Serwist caching with a custom strategy matrix tuned for the MultiRotter resource types.

Purpose: This is the PWA foundation — manifest makes the app installable, caching strategies make it fast and offline-capable.
Output: manifest.ts, updated sw.ts with custom caching, PWA icon PNGs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing service worker structure -->
From src/app/sw.ts:
```typescript
import { defaultCache } from "@serwist/next/worker";
import { Serwist } from "serwist";
import type { PrecacheEntry, SerwistGlobalConfig } from "serwist";
// Currently uses defaultCache — must be replaced with custom runtimeCaching
```

From next.config.ts:
```typescript
import withSerwistInit from "@serwist/next";
const withSerwist = withSerwistInit({
  swSrc: "src/app/sw.ts",
  swDest: "public/sw.js",
});
export default withSerwist(nextConfig);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Web app manifest and PWA icons</name>
  <files>src/app/manifest.ts, public/icons/icon-192x192.png, public/icons/icon-512x512.png</files>
  <action>
    Create `src/app/manifest.ts` exporting a default function that returns a `MetadataRoute.Manifest` object (per D-01 CONTEXT.md decisions):
    - name: "MultiRotter"
    - short_name: "MultiRotter"
    - description: "Breaking news scoops in real-time — retro forum PWA"
    - start_url: "/"
    - display: "standalone"
    - orientation: "any"
    - theme_color: "#71B7E6"
    - background_color: "#FFFFFF"
    - icons array with two entries:
      - { src: "/icons/icon-192x192.png", sizes: "192x192", type: "image/png" }
      - { src: "/icons/icon-512x512.png", sizes: "512x512", type: "image/png", purpose: "maskable" }

    For the icon PNGs, generate them programmatically using a Node.js script that creates simple colored square PNGs (theme blue #71B7E6 background with white "MR" text centered). Use Canvas API if available, or create minimal valid PNG files with solid #71B7E6 fill. The icons must be valid PNG files at the exact pixel dimensions.

    Alternative if canvas is unavailable: create a simple script using raw PNG binary construction (IHDR + IDAT with zlib deflate) for a solid #71B7E6 square. The icon just needs to be a valid installable-size PNG.

    Import type: `import type { MetadataRoute } from "next";`
  </action>
  <verify>
    <automated>node -e "const m = require('./src/app/manifest.ts'); console.log('skip')" 2>/dev/null; npx tsc --noEmit src/app/manifest.ts 2>&1 | head -5; test -f public/icons/icon-192x192.png && echo "192 icon exists" || echo "MISSING 192 icon"; test -f public/icons/icon-512x512.png && echo "512 icon exists" || echo "MISSING 512 icon"</automated>
  </verify>
  <done>manifest.ts exports valid MetadataRoute.Manifest with name "MultiRotter", theme_color "#71B7E6", two icon entries. Both PNG files exist at correct sizes.</done>
</task>

<task type="auto">
  <name>Task 2: Custom Serwist caching strategy matrix</name>
  <files>src/app/sw.ts</files>
  <action>
    Replace `defaultCache` import and usage in `src/app/sw.ts` with a custom `runtimeCaching` array. Keep all existing type declarations and the Serwist constructor pattern.

    The custom runtimeCaching array must contain these strategies (use Serwist v9 API — import from "serwist"):

    1. **API routes — NetworkFirst**:
       - urlPattern: /\/api\/.*/i
       - handler: "NetworkFirst"
       - options: { cacheName: "api-cache", expiration: { maxEntries: 64, maxAgeSeconds: 24 * 60 * 60 }, networkTimeoutSeconds: 10 }

    2. **Static build assets — CacheFirst**:
       - urlPattern: /\/_next\/static\/.*/i
       - handler: "CacheFirst"
       - options: { cacheName: "static-cache", expiration: { maxEntries: 128, maxAgeSeconds: 365 * 24 * 60 * 60 } }

    3. **Image/SVG assets — CacheFirst**:
       - urlPattern: /\.(?:png|gif|jpg|jpeg|svg|webp|ico)$/i
       - handler: "CacheFirst"
       - options: { cacheName: "image-cache", expiration: { maxEntries: 64, maxAgeSeconds: 30 * 24 * 60 * 60 } }

    4. **HTML navigations — StaleWhileRevalidate** (as offline-capable fallback):
       - urlPattern: /^https?:\/\/.*/i (catch-all for navigations)
       - handler: "StaleWhileRevalidate"
       - options: { cacheName: "page-cache", expiration: { maxEntries: 32, maxAgeSeconds: 24 * 60 * 60 } }

    Remove the `import { defaultCache } from "@serwist/next/worker"` line. Keep `navigationPreload: true`, `skipWaiting: true`, `clientsClaim: true`.

    The runtimeCaching entries use the Serwist RuntimeCaching type. Import it if needed. Order matters — more specific patterns first (API, static, images) before the catch-all.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/app/sw.ts 2>&1 | head -10; pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>sw.ts compiles with zero errors. Build produces public/sw.js containing "NetworkFirst", "CacheFirst", "StaleWhileRevalidate" strings. No reference to defaultCache remains.</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds without errors
- `grep -c "NetworkFirst" public/sw.js` returns >= 1
- `grep -c "defaultCache" src/app/sw.ts` returns 0
- manifest.ts type-checks with `npx tsc --noEmit`
- Both icon PNGs exist in public/icons/
</verification>

<success_criteria>
- Web app manifest is served at /manifest.webmanifest with correct name, theme_color, and icon references
- Service worker uses NetworkFirst for API, CacheFirst for static/images, StaleWhileRevalidate for pages
- PWA icons at 192x192 and 512x512 exist as valid PNGs
</success_criteria>

<output>
After completion, create `.planning/phases/08-pwa-completion/08-01-SUMMARY.md`
</output>
